# CodeCoreAgent 函数与类注解（全量）

对照 **当前仓库源码 0.36.0** 自动抽取。每个公开类 / 函数给出：

- **位置**：文件与行号
- **签名**：参数与返回类型（以源码注解为准）
- **功能说明**：优先用源码 docstring；没有则按命名约定补中文说明
- **方法**：类的公开方法（含 `__init__`）同样列出

下划线开头的内部辅助函数默认不收录，避免把实现细节当成稳定 API。
生成器：`scripts/build_full_archive.py`。

共扫描 `105` 个 `.py` 文件。
统计：公开类 **153**，模块级函数 **155**，公开方法 **359**。


## `src/codeagent/__init__.py`

> 模块说明：codeagent: a professional framework for building code development agents.

（无公开类或函数；文件以常量、类型别名或再导出为主。）

## `src/codeagent/blueprint/__init__.py`

> 模块说明：Blueprint: free-form DAG orchestration with approval gates (HiveWard-style).

（无公开类或函数；文件以常量、类型别名或再导出为主。）

## `src/codeagent/blueprint/graph.py`

> 模块说明：Blueprint graph: a DAG of agent/manager/slot/condition/approval/aggregate nodes. Inspired by HiveWard's blueprint canvas: instead of a fixed pipeline, the workflow is a free-form directed acyclic graph. Upstream outputs flow into downstream nodes along edges; conditions and managers decide which branches activate; approvals pause for a human decision.

### 类 `NodeKind`

- **基类**：`str, Enum`
- **定义行**：16
- **功能说明**：十种节点类型枚举。

### 类 `Node`

- **基类**：`object`
- **定义行**：31
- **功能说明**：蓝图节点（种类、提示词、标签）。

### 类 `Edge`

- **基类**：`object`
- **定义行**：48
- **功能说明**：蓝图连线，可带条件标签。

### 类 `CycleError`

- **基类**：`ValueError`
- **定义行**：54
- **功能说明**：Raised when the blueprint graph contains a cycle.

### 类 `Blueprint`

- **基类**：`object`
- **定义行**：58
- **功能说明**：A validated DAG of nodes and edges.

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def __init__(self, nodes: list[Node], edges: list[Edge], name: str = 'blueprint') -> None` | 61 | 构造并保存依赖（Provider、路径、配置等）。 |
| `def upstream(self, node_id: str) -> list[str]` | 84 | 返回指向该节点的上游节点 id。 |
| `def downstream(self, node_id: str) -> list[str]` | 87 | 返回该节点的下游节点 id。 |
| `def downstream_edges(self, node_id: str) -> list[Edge]` | 90 | 返回从该节点出发的边。 |
| `def roots(self) -> list[str]` | 93 | 没有入边的起始节点。 |
| `def leaves(self) -> list[str]` | 96 | 没有出边的结束节点。 |
| `def topo_levels(self) -> list[list[str]]` | 116 | Group node ids into parallel-executable topological levels. |

## `src/codeagent/blueprint/runner.py`

> 模块说明：Blueprint runner: executes a blueprint DAG with parallelism and approval gates.

### 类 `NodeResult`

- **基类**：`object`
- **定义行**：17
- **功能说明**：`NodeResult`：该模块的数据结构或服务类型，详见源码 docstring 与调用处。

### 类 `ApprovalResult`

- **基类**：`object`
- **定义行**：25
- **功能说明**：Human decision at an approval gate.

### 类 `BlueprintReport`

- **基类**：`object`
- **定义行**：33
- **功能说明**：`BlueprintReport`：该模块的数据结构或服务类型，详见源码 docstring 与调用处。

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def outputs(self) -> dict[str, str]` | 39 | `outputs`：该模块的公开接口，参数见签名，行为见同行源码。 |
| `def final_output(self, blueprint: Blueprint) -> str` | 42 | Joined outputs of leaf nodes (the blueprint's deliverables). |
| `def scorecard(self, blueprint: Blueprint) -> dict[str, Any]` | 51 | Per-run scorecard: node outcomes, attempts, token usage (HomeRail-style). |

### 类 `BlueprintRunner`

- **基类**：`object`
- **定义行**：129
- **功能说明**：Runs a :class:`Blueprint` level by level, parallel within a level. Parameters ---------- provider: Default LLM backend for agent/manager/condition/aggregate nodes. blueprint: The validated DAG to execute. approval_handler: Called at APPROVAL nodes. ``None`` auto-approves (permissive). audit: Optional audit log with a ``record(kind, **detail)`` method (e.g. :class:`codeagent.court.audit.AuditLog`). providers: Optional per-node provider mapping, keyed by node id with ``"*"`` as fallback — "smart brain, efficient workers" (HomeRail-style). command_allowlist: Executables a COMMAND node may run (no shell). Empty = all denied.

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def __init__(self, provider: LLMProvider, blueprint: Blueprint, approval_handler: ApprovalHandler | None = None, audit: Any = None, providers: dict[str, LLMProvider] | None = None, command_allowlist: list[str] | None = None) -> None` | 150 | 构造并保存依赖（Provider、路径、配置等）。 |
| `async def run(self, task: str) -> BlueprintReport` | 181 | 执行主流程并返回结果。 |

## `src/codeagent/blueprint/yaml_loader.py`

> 模块说明：Load a Blueprint from YAML with strict validation. Follows HomeRail's WorkflowSpec lesson: unknown fields are hard errors, so a typo can never silently change what a workflow does.

### 类 `BlueprintSpecError`

- **基类**：`ValueError`
- **定义行**：19
- **功能说明**：Raised when a blueprint YAML document fails strict validation.

### `def parse_blueprint(data: dict[str, Any]) -> Blueprint`

- **定义行**：31
- **功能说明**：Build a :class:`Blueprint` from an already-parsed mapping.

### `def load_blueprint_yaml(source: str | Path) -> Blueprint`

- **定义行**：91
- **功能说明**：Load a blueprint from a YAML file path or YAML text.

## `src/codeagent/cli/__init__.py`

（无公开类或函数；文件以常量、类型别名或再导出为主。）

## `src/codeagent/cli/main.py`

> 模块说明：Command-line interface for codeagent.

### `def run(task: str = typer.Argument(..., help='The coding task for the agent to perform.'), provider: str = typer.Option('anthropic', '--provider', '-p', help=f'LLM provider ({', '.join(list_providers())}); comma-separate for aggregation, e.g. ollama,openrouter.'), model: str | None = typer.Option(None, '--model', '-m', help='Model name (provider default if omitted).'), api_key: str | None = typer.Option(None, '--api-key', envvar='CODEAGENT_API_KEY', help='API key (defaults to provider env var).'), base_url: str | None = typer.Option(None, '--base-url', help='Custom API base URL.'), strategy: str = typer.Option('fallback', '--strategy', help='Aggregation strategy for comma-separated providers: fallback | round-robin.'), root: Path = typer.Option(Path.cwd(), '--root', '-r', help='Workspace root the agent may touch.'), max_iterations: int = typer.Option(50, '--max-iterations', help='Safety cap on model/tool round-trips.'), budget: int | None = typer.Option(None, '--budget', help='Max total tokens (input+output), sub-agents included.'), mcp_config: Path | None = typer.Option(None, '--mcp-config', help='Path to a Claude-style MCP servers JSON file.'), web: bool = typer.Option(False, '--web', help='Enable web scraping tools (web_fetch / web_scrape).'), ocr: bool = typer.Option(False, '--ocr', help='Enable the OCR tool (image text extraction).'), skills_dir: Path | None = typer.Option(None, '--skills', help='Directory of SKILL.md packs to load (default: ~/.codeagent/skills if present).'), evolve: bool = typer.Option(False, '--evolve', help='Self-evolution: distill each finished task into a reusable skill.'), yes: bool = typer.Option(False, '--yes', '-y', help='Auto-approve all tool calls, including destructive ones.'), verbose: bool = typer.Option(False, '--verbose', '-v', help='Show full tool results.')) -> None`

- **定义行**：192
- **功能说明**：Run the agent on a single task and print the final answer.

### `def chat(provider: str = typer.Option('anthropic', '--provider', '-p', help=f'LLM provider ({', '.join(list_providers())}); comma-separate for aggregation, e.g. ollama,openrouter.'), model: str | None = typer.Option(None, '--model', '-m', help='Model name (provider default if omitted).'), api_key: str | None = typer.Option(None, '--api-key', envvar='CODEAGENT_API_KEY', help='API key (defaults to provider env var).'), base_url: str | None = typer.Option(None, '--base-url', help='Custom API base URL.'), strategy: str = typer.Option('fallback', '--strategy', help='Aggregation strategy for comma-separated providers: fallback | round-robin.'), root: Path = typer.Option(Path.cwd(), '--root', '-r', help='Workspace root the agent may touch.'), max_iterations: int = typer.Option(50, '--max-iterations', help='Safety cap per turn.'), budget: int | None = typer.Option(None, '--budget', help='Max total tokens (input+output), sub-agents included.'), mcp_config: Path | None = typer.Option(None, '--mcp-config', help='Path to a Claude-style MCP servers JSON file.'), web: bool = typer.Option(False, '--web', help='Enable web scraping tools (web_fetch / web_scrape).'), ocr: bool = typer.Option(False, '--ocr', help='Enable the OCR tool (image text extraction).'), skills_dir: Path | None = typer.Option(None, '--skills', help='Directory of SKILL.md packs to load (default: ~/.codeagent/skills if present).'), evolve: bool = typer.Option(False, '--evolve', help='Self-evolution: distill each finished task into a reusable skill.'), yes: bool = typer.Option(False, '--yes', '-y', help='Auto-approve all tool calls, including destructive ones.'), verbose: bool = typer.Option(False, '--verbose', '-v', help='Show full tool results.'), emotion: bool = typer.Option(False, '--emotion', help='Emotional-companion mode: replies carry an emotion tag.'), voice: bool = typer.Option(False, '--voice', help='Speak replies aloud with the free edge-tts voice pack (implies --emotion).'), voice_name: str | None = typer.Option(None, '--voice-name', help='TTS voice: preset (xiaoxiao/xiaoyi/yunxi/yunjian/xiaochen/hsiaochen/hsiaoyu/yunjhe) or full edge-tts voice ID.'), mic: bool = typer.Option(False, '--mic', help='Full voice loop: push-to-talk microphone input via local Whisper ASR.'), mic_model: str = typer.Option('base', '--mic-model', help='Whisper model size: tiny/base/small/medium/large-v3.'), memory: bool = typer.Option(False, '--memory', help='Persist long-term memory across sessions (~/.codeagent/memory.json).')) -> None`

- **定义行**：248
- **功能说明**：Start an interactive multi-turn chat session with the agent.

### `def lead(workers_file: Path | None = typer.Option(None, '--workers', '-w', help='Workers YAML roster (name/provider/model/description per worker).'), provider: str = typer.Option('anthropic', '--provider', '-p', help=f'Leader LLM provider ({', '.join(list_providers())}); comma-separate for aggregation.'), model: str | None = typer.Option(None, '--model', '-m', help='Leader model name.'), api_key: str | None = typer.Option(None, '--api-key', envvar='CODEAGENT_API_KEY', help='API key (defaults to provider env var).'), base_url: str | None = typer.Option(None, '--base-url', help='Custom API base URL.'), strategy: str = typer.Option('fallback', '--strategy', help='Aggregation strategy for comma-separated providers: fallback | round-robin.'), root: Path = typer.Option(Path.cwd(), '--root', '-r', help='Project root the workers operate on.'), max_iterations: int = typer.Option(30, '--max-iterations', help='Safety cap per worker task.'), web: bool = typer.Option(False, '--web', help='Give workers web scraping tools.'), ocr: bool = typer.Option(False, '--ocr', help='Give workers the OCR tool.'), skills_dir: Path | None = typer.Option(None, '--skills', help='Skill packs directory (default: ~/.codeagent/skills if present).'), voice: bool = typer.Option(False, '--voice', help="Speak the leader's replies aloud (edge-tts)."), voice_name: str | None = typer.Option(None, '--voice-name', help='TTS voice preset or full edge-tts voice ID.'), mic: bool = typer.Option(False, '--mic', help='Voice commands via push-to-talk microphone (local Whisper ASR).'), mic_model: str = typer.Option('base', '--mic-model', help='Whisper model size.'), no_escalate: bool = typer.Option(False, '--no-escalate', help='Disable escalation to external agent platforms on worker failure.')) -> None`

- **定义行**：367
- **功能说明**：Lead a team of worker models: voice/text commands, live progress.

### `def settings_show() -> None`

- **定义行**：482
- **功能说明**：Show version info and the current personalization settings.

### `def settings_set(nickname: str | None = typer.Option(None, '--nickname', '-n', help='怎么称呼你（如：石头）。'), language: str | None = typer.Option(None, '--language', '-l', help='首选回复语言（如：中文）。'), instructions: str | None = typer.Option(None, '--instructions', '-i', help='额外行为说明（如：回答先给结论，再给细节）。'), context: str | None = typer.Option(None, '--context', '-c', help='用户与主机上下文（如：M4 Mac，项目在 ~/code）。')) -> None`

- **定义行**：497
- **功能说明**：Set personalization fields (only provided fields are updated).

### `def settings_clear() -> None`

- **定义行**：519
- **功能说明**：Remove all personalization settings.

### `def desktop_cmd(root: Path = typer.Option(Path.cwd(), '--root', '-r', help='Workspace root the agent may touch.')) -> None`

- **定义行**：528
- **功能说明**：Launch the desktop UI (chat window, settings panel, version info).

### `def version_cmd() -> None`

- **定义行**：538
- **功能说明**：Show the current version and what changed in this release.

### `def changelog_cmd() -> None`

- **定义行**：548
- **功能说明**：Show the full per-release update history.

### `def logs_cmd(lines: int = typer.Option(50, '--lines', '-n', help='Number of recent log lines to show.')) -> None`

- **定义行**：554
- **功能说明**：Show recent run logs (~/.codeagent/logs/codeagent.log).

## `src/codeagent/core/__init__.py`

（无公开类或函数；文件以常量、类型别名或再导出为主。）

## `src/codeagent/core/agent.py`

> 模块说明：The agent loop: send messages, dispatch tool calls, feed results back.

### 类 `AgentEvent`

- **基类**：`object`
- **定义行**：51
- **功能说明**：循环向外抛的事件（文本、工具、审批、完成、错误）。

### 类 `MaxIterationsError`

- **基类**：`RuntimeError`
- **定义行**：56
- **功能说明**：Raised when the agent exceeds its iteration budget.

### 类 `Agent`

- **基类**：`object`
- **定义行**：60
- **功能说明**：A tool-using coding agent driven by an LLM provider. Parameters ---------- provider: The LLM backend used for completions. tools: Registry of tools the agent may call. system_prompt: Overrides the default coding-agent system prompt. max_iterations: Safety cap on model/tool round-trips per ``run``. permissions: Optional :class:`PermissionPolicy` gating tool calls by risk. ``None`` means permissive (everything is approved). budget: Optional token :class:`Budget`; sub-agent usage rolls up into it. compactor: Optional :class:`ConversationCompactor`; when history grows past its threshold, older turns are summarized (short-term memory). memory: Optional :class:`MemoryStore`; memories relevant to each task are injected into the system prompt (long-term memory recall). skills: Optional :class:`SkillLibrary`; skill catalog is always visible, matching skills auto-activate, and ``use_skill`` can load any pack. workspace_hints: Optional project/file-type text used to recognize work content. settings: Optional host-wide personalization :class:`Settings`; extra instructions and context injected into every conversation. skill_evolver: Optional :class:`SkillEvolver`; after each successful run it reflects on the experience and may write a new skill. on_event: Optional callback receiving :class:`AgentEvent` objects for streaming UIs, logging, or human-in-the-loop approval.

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def __init__(self, provider: LLMProvider, tools: ToolRegistry | None = None, system_prompt: str | None = None, max_iterations: int = 50, permissions: PermissionPolicy | None = None, budget: Budget | None = None, compactor: 'ConversationCompactor | None' = None, memory: 'MemoryStore | None' = None, skills: 'SkillLibrary | None' = None, settings: 'Settings | None' = None, skill_evolver: 'SkillEvolver | None' = None, on_event: EventHandler | None = None, workspace_hints: str = '') -> None` | 100 | 构造并保存依赖（Provider、路径、配置等）。 |
| `def usage(self) -> Usage` | 138 | Total token usage, including delegated sub-agents. |
| `def total_usage(self) -> Usage` | 143 | `total_usage`：该模块的公开接口，参数见签名，行为见同行源码。 |
| `def reset(self) -> None` | 157 | Clear conversation history and token usage. |
| `def invoke_skills(self, names: list[str]) -> list['Skill']` | 176 | Mark skills as user-invoked (or model-invoked) for this session. |
| `async def run(self, task: str) -> str` | 268 | Run the agent on a task until the model stops calling tools. Returns the final assistant text. Raises :class:`MaxIterationsError` if the iteration budget is exhausted, or :class:`BudgetExceededError` if the token budget is exceeded. |

## `src/codeagent/core/budget.py`

> 模块说明：Token budgets for agent runs, including sub-agent roll-up.

### 类 `BudgetExceededError`

- **基类**：`RuntimeError`
- **定义行**：10
- **功能说明**：Raised when an agent's total token usage exceeds its budget.

### 类 `Budget`

- **基类**：`object`
- **定义行**：15
- **功能说明**：A spending cap for an agent run. ``max_total_tokens`` counts input + output tokens, including usage rolled up from delegated sub-agents (Codex-style root-goal budgeting). When ``aware`` is set, the agent injects a budget-status line into the system prompt on every iteration, so the model can adapt its behavior (pace exploration, prefer verification over blind search) as the budget drains — the key mechanism behind Google Research's Budget-Aware Tool-Use (BATS) agent scaling.

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def exceeded(self, usage: Usage) -> bool` | 31 | `exceeded`：该模块的公开接口，参数见签名，行为见同行源码。 |
| `def status_message(self, usage: Usage) -> str` | 36 | One-line budget status for in-context injection. |

## `src/codeagent/core/config.py`

> 模块说明：Configuration for building an agent from declarative settings.

### 类 `AgentConfig`

- **基类**：`BaseModel`
- **定义行**：11
- **功能说明**：Everything needed to construct an :class:`~codeagent.core.agent.Agent`.

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def build_agent(self, **agent_kwargs)` | 31 | Construct an Agent from this config. Extra keyword arguments (e.g. ``on_event``) are forwarded to the Agent constructor. MCP servers from ``mcp_config`` are not connected here — use :class:`codeagent.mcp.MCPManager` as an async context. |

## `src/codeagent/core/types.py`

> 模块说明：Unified message and response types shared across all LLM providers.

### 类 `Role`

- **基类**：`str, Enum`
- **定义行**：11
- **功能说明**：消息角色：user / assistant / system / tool。

### 类 `ToolCall`

- **基类**：`object`
- **定义行**：19
- **功能说明**：A request from the model to invoke a tool.

### 类 `ToolResult`

- **基类**：`object`
- **定义行**：28
- **功能说明**：The outcome of executing a tool call.

### 类 `Message`

- **基类**：`object`
- **定义行**：37
- **功能说明**：A single turn in the conversation. - USER / SYSTEM messages carry ``content``. - ASSISTANT messages carry ``content`` and optionally ``tool_calls``. - TOOL messages carry ``tool_results`` from one tool-execution round.

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def user(cls, content: str) -> Message` | 51 | `user`：该模块的公开接口，参数见签名，行为见同行源码。 |
| `def system(cls, content: str) -> Message` | 55 | `system`：该模块的公开接口，参数见签名，行为见同行源码。 |
| `def assistant(cls, content: str = '', tool_calls: list[ToolCall] | None = None) -> Message` | 59 | `assistant`：该模块的公开接口，参数见签名，行为见同行源码。 |
| `def tool(cls, results: list[ToolResult]) -> Message` | 63 | `tool`：该模块的公开接口，参数见签名，行为见同行源码。 |

### 类 `Usage`

- **基类**：`object`
- **定义行**：68
- **功能说明**：一次 complete 的 token 计数。

### 类 `LLMResponse`

- **基类**：`object`
- **定义行**：80
- **功能说明**：Normalized response returned by every provider.

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def wants_tool_use(self) -> bool` | 89 | `wants_tool_use`：该模块的公开接口，参数见签名，行为见同行源码。 |

## `src/codeagent/court/__init__.py`

> 模块说明：Court: 三省六部 multi-agent orchestration (plan → review → execute).

（无公开类或函数；文件以常量、类型别名或再导出为主。）

## `src/codeagent/court/audit.py`

> 模块说明：Audit trail: every state transition and decision, persisted as JSONL. The 奏折存档 (memorial archive) equivalent — full traceability of who did what, when, and why, so any edict's handling can be replayed and audited.

### 类 `AuditEvent`

- **基类**：`object`
- **定义行**：17
- **功能说明**：`AuditEvent`：该模块的数据结构或服务类型，详见源码 docstring 与调用处。

### 类 `AuditLog`

- **基类**：`object`
- **定义行**：23
- **功能说明**：In-memory event list, optionally mirrored to a JSONL file.

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def __init__(self, path: str | Path | None = None) -> None` | 26 | 构造并保存依赖（Provider、路径、配置等）。 |
| `def record(self, kind: str, **detail) -> AuditEvent` | 32 | `record`：该模块的公开接口，参数见签名，行为见同行源码。 |
| `def of_kind(self, kind: str) -> list[AuditEvent]` | 40 | `of_kind`：该模块的公开接口，参数见签名，行为见同行源码。 |

## `src/codeagent/court/roles.py`

> 模块说明：Court roles: specialized agent personas modeled on 三省六部制. Each role is a named persona (system prompt + duty) that the workflow instantiates as a sub-agent. The split of powers is the point: - 中书省 (zhongshu) plans — but cannot execute. - 门下省 (menxia) reviews — and can reject (封驳) the plan, forcing rework. - 尚书省 (shangshu) dispatches and aggregates — but does not plan. - 六部 (six ministries) execute — each in its own specialty only.

### 类 `CourtRole`

- **基类**：`object`
- **定义行**：18
- **功能说明**：`CourtRole`：该模块的数据结构或服务类型，详见源码 docstring 与调用处。

## `src/codeagent/court/state.py`

> 模块说明：Task state machine with protected transitions. Illegal jumps (e.g. DOING back to PLANNING) are rejected, so the workflow cannot silently bypass the review gate — the institutional guarantee that makes the pipeline trustworthy.

### 类 `TaskState`

- **基类**：`str, Enum`
- **定义行**：13
- **功能说明**：`TaskState`：该模块的数据结构或服务类型，详见源码 docstring 与调用处。

### 类 `IllegalTransitionError`

- **基类**：`RuntimeError`
- **定义行**：42
- **功能说明**：`IllegalTransitionError`：该模块的数据结构或服务类型，详见源码 docstring 与调用处。

### 类 `TaskStateMachine`

- **基类**：`object`
- **定义行**：46
- **功能说明**：Tracks one edict's state; every transition is validated.

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def __init__(self) -> None` | 49 | 构造并保存依赖（Provider、路径、配置等）。 |
| `def transition(self, target: TaskState) -> TaskState` | 53 | `transition`：该模块的公开接口，参数见签名，行为见同行源码。 |

## `src/codeagent/court/workflow.py`

> 模块说明：The Court: 三省六部 multi-agent orchestration workflow. 一道旨意的完整旅程： 旨意 → 中书省规划 → 门下省审议（可封驳，打回重规划） → 尚书省派发 → 六部并行执行 → 尚书省汇总回奏 Every step is validated by a protected state machine and written to an audit trail, so results are reproducible, auditable, and reviewable — the institutional quality gate that plain "agents chatting" lacks.

### 类 `SubTask`

- **基类**：`object`
- **定义行**：39
- **功能说明**：`SubTask`：该模块的数据结构或服务类型，详见源码 docstring 与调用处。

### 类 `SubTaskResult`

- **基类**：`object`
- **定义行**：46
- **功能说明**：`SubTaskResult`：该模块的数据结构或服务类型，详见源码 docstring 与调用处。

### 类 `CourtReport`

- **基类**：`object`
- **定义行**：53
- **功能说明**：`CourtReport`：该模块的数据结构或服务类型，详见源码 docstring 与调用处。

### 类 `ReviewRejectedError`

- **基类**：`RuntimeError`
- **定义行**：63
- **功能说明**：Strict mode: the plan was still rejected after all review rounds.

### 类 `Court`

- **基类**：`object`
- **定义行**：67
- **功能说明**：Orchestrates role agents through the plan→review→execute pipeline.

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def __init__(self, provider: LLMProvider, roles: dict[str, CourtRole] | None = None, tools: ToolRegistry | None = None, max_review_rounds: int = 3, strict_review: bool = False, audit_path: str | Path | None = None, max_subtask_iterations: int = 20) -> None` | 70 | 构造并保存依赖（Provider、路径、配置等）。 |
| `async def run(self, edict: str) -> CourtReport` | 99 | 执行主流程并返回结果。 |

## `src/codeagent/desktop/__init__.py`

（无公开类或函数；文件以常量、类型别名或再导出为主。）

## `src/codeagent/desktop/activity.py`

> 模块说明：Activity stream & self-learning records (LCA Learning.tsx style). 活动流是全系统统一的事件条带（kind 分类 + 文本）；学习信号藏在 kind == "learn" 的条目里——赞/踩、学习完成、进化作业都走这条流。 统计是纯函数式现算：不维护中间计数器，活动流即真相。

### 类 `LearnRecord`

- **基类**：`object`
- **定义行**：68
- **功能说明**：`LearnRecord`：该模块的数据结构或服务类型，详见源码 docstring 与调用处。

### 类 `LearningStore`

- **基类**：`object`
- **定义行**：77
- **功能说明**：`LearningStore`：该模块的数据结构或服务类型，详见源码 docstring 与调用处。

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def load(cls, path: Path | None = None) -> 'LearningStore'` | 81 | 从磁盘或默认路径加载持久化数据。 |
| `def save(self, path: Path | None = None) -> None` | 91 | 把当前对象写回本机配置文件。 |
| `def upsert_today(self, record: LearnRecord) -> None` | 100 | 同日覆盖、异日追加（LCA learnNow 语义）。 |

### `def log_activity(kind: str, text: str, path: Path | None = None) -> dict[str, Any]`

- **定义行**：23
- **功能说明**：Append one entry to the activity stream (user-visible trace).

### `def read_activity(limit: int = 100, path: Path | None = None) -> list[dict[str, Any]]`

- **定义行**：48
- **功能说明**：`read_activity`：该模块的公开接口，参数见签名，行为见同行源码。

### `def record_feedback(positive: bool, path: Path | None = None) -> dict[str, Any]`

- **定义行**：61
- **功能说明**：对话页赞/踩 → kind=learn 条目（LCA：赞记「正向」，踩记「点踩」）。

### `def learn_now(activity_path: Path | None = None, learning_path: Path | None = None) -> dict[str, Any]`

- **定义行**：107
- **功能说明**：从活动流现算当日准确率并 upsert 学习记录（活动流即真相）。

## `src/codeagent/desktop/api.py`

> 模块说明：JS bridge for the desktop UI. A ``DesktopAPI`` instance is exposed to the webview's JavaScript as ``pywebview.api.*``. Python pushes live updates back via ``window.evaluate_js``. No web server involved. UI preferences persist to ``~/.codeagent/desktop.json``; personalization reuses the host-wide ``Settings`` store.

### 类 `DesktopConfig`

- **基类**：`object`
- **定义行**：104
- **功能说明**：Everything configurable from the UI.

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def load(cls, path: Path | None = None) -> 'DesktopConfig'` | 126 | 从磁盘或默认路径加载持久化数据。 |
| `def save(self, path: Path | None = None) -> None` | 137 | 把当前对象写回本机配置文件。 |

### 类 `DesktopAPI`

- **基类**：`object`
- **定义行**：144
- **功能说明**：Object exposed to JavaScript. All methods are callable from JS.

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def __init__(self, root: Path | None = None) -> None` | 147 | 构造并保存依赖（Provider、路径、配置等）。 |
| `def get_state(self) -> dict[str, Any]` | 212 | 按键或标识读取一项。 |
| `def get_privacy_policy(self) -> dict[str, Any]` | 238 | Current privacy policy + whether this install has accepted it. |
| `def accept_privacy(self) -> dict[str, Any]` | 246 | Record acceptance of the current privacy policy version. |
| `def get_changelog(self) -> str` | 272 | 按键或标识读取一项。 |
| `def get_nav_status(self) -> dict[str, Any]` | 275 | 侧栏底部：本地端点/探测模型、API 在线、聚合池启用、当前激活。 |
| `def get_overview(self) -> dict[str, Any]` | 295 | Dashboard stats: counts of skills, memories, runs, harnesses. |
| `def save_config(self, data: dict[str, Any]) -> dict[str, Any]` | 319 | 把当前对象写回本机配置文件。 |
| `def save_settings(self, data: dict[str, Any]) -> dict[str, Any]` | 347 | 把当前对象写回本机配置文件。 |
| `def get_model_assets(self) -> dict[str, Any]` | 360 | 按键或标识读取一项。 |
| `def add_endpoint(self, base: str, label: str = '', role: str = 'backup') -> dict[str, Any]` | 471 | 新增一条记录。 |
| `def remove_endpoint(self, endpoint_id: str) -> bool` | 491 | `remove_endpoint`：该模块的公开接口，参数见签名，行为见同行源码。 |
| `def detect_models(self, base_url: str, api_key: str = '') -> dict[str, Any]` | 501 | Dual-protocol sniff of a Base URL (OpenAI /models → /api/tags)。 带 API Key 识别：多数提供方的 /models 需要鉴权。 |
| `def add_api_model(self, base_url: str, model: str, label: str = '', api_key: str = '', provider: str = '') -> dict[str, Any]` | 509 | 新增一条记录。 |
| `def remove_api_model(self, model_id: str) -> bool` | 525 | `remove_api_model`：该模块的公开接口，参数见签名，行为见同行源码。 |
| `def set_active_model(self, ref: str) -> dict[str, Any]` | 537 | ref: 'local:model@endpoint_id' \| 'api:{id}' \| 'mix:{id}' \| 'route:free'. Gradio 端点可选为当前「文生视频」模型，但不能作为对话 LLM。 空字符串与 ``route:free`` 都表示自由路由。 |
| `def test_model(self, ref: str) -> dict[str, Any]` | 552 | Connectivity test through the same provider path as real chat. |
| `def get_models_page(self) -> dict[str, Any]` | 578 | Local tab data: endpoints with detailed tags + running status. |
| `def set_local_loaded(self, endpoint_id: str, model: str, load: bool) -> dict[str, Any]` | 653 | 写入或更新一项。 |
| `def delete_local_model(self, endpoint_id: str, model: str) -> dict[str, Any]` | 665 | 删除一条记录。 |
| `def pull_model(self, model: str) -> dict[str, Any]` | 681 | 部署新模型：后台线程拉取（主推理端点优先）。 |
| `def save_mixture(self, name: str, strategy: str, members: list[str], mix_id: str = '') -> dict[str, Any]` | 704 | 把当前对象写回本机配置文件。 |
| `def delete_mixture(self, mix_id: str) -> bool` | 727 | 删除一条记录。 |
| `def toggle_mixture(self, mix_id: str, enabled: bool) -> bool` | 738 | `toggle_mixture`：该模块的公开接口，参数见签名，行为见同行源码。 |
| `def get_router(self) -> dict[str, Any]` | 786 | 按键或标识读取一项。 |
| `def add_route_rule(self, task_type: str, keywords: str, target: str) -> dict[str, Any]` | 802 | 新增一条记录。 |
| `def delete_route_rule(self, rule_id: str) -> bool` | 826 | 删除一条记录。 |
| `def toggle_route_rule(self, rule_id: str, enabled: bool) -> bool` | 834 | `toggle_route_rule`：该模块的公开接口，参数见签名，行为见同行源码。 |
| `def move_route_rule(self, rule_id: str, direction: int) -> bool` | 842 | `move_route_rule`：该模块的公开接口，参数见签名，行为见同行源码。 |
| `def save_route_weights(self, cost: int, quality: int, local_first: int) -> dict[str, Any]` | 857 | 把当前对象写回本机配置文件。 |
| `def route_sandbox(self, text: str) -> dict[str, Any]` | 864 | 按规则为文本选择模型。 |
| `def get_permissions(self) -> dict[str, Any]` | 876 | 按键或标识读取一项。 |
| `def set_permission_level(self, cap_id: str, level: str) -> dict[str, Any]` | 887 | 写入或更新一项。 |
| `def resolve_confirm(self, cid: str, approved: bool) -> bool` | 897 | `resolve_confirm`：该模块的公开接口，参数见签名，行为见同行源码。 |
| `def get_versions(self) -> dict[str, Any]` | 904 | 按键或标识读取一项。 |
| `def get_activity(self, limit: int = 80) -> list[dict[str, Any]]` | 922 | 按键或标识读取一项。 |
| `def send_feedback(self, positive: bool) -> dict[str, Any]` | 925 | 对话页赞/踩 → 活动流 kind=learn 条目。 |
| `def get_learning(self) -> dict[str, Any]` | 930 | 按键或标识读取一项。 |
| `def learn_now(self) -> dict[str, Any]` | 946 | `learn_now`：该模块的公开接口，参数见签名，行为见同行源码。 |
| `def get_workflows(self) -> list[dict[str, Any]]` | 960 | 按键或标识读取一项。 |
| `def add_workflow(self, name: str, desc: str, trigger: str, steps: list[dict[str, str]]) -> dict[str, Any]` | 972 | 新增一条记录。 |
| `def delete_workflow(self, wf_id: str) -> bool` | 991 | 删除一条记录。 |
| `def run_workflow(self, wf_id: str) -> dict[str, Any]` | 1000 | 执行主流程并返回结果。 |
| `def pause_workflow(self, wf_id: str) -> bool` | 1007 | `pause_workflow`：该模块的公开接口，参数见签名，行为见同行源码。 |
| `def set_workflow_continuous(self, wf_id: str, on: bool) -> bool` | 1010 | 写入或更新一项。 |
| `def get_cron(self) -> dict[str, Any]` | 1038 | 按键或标识读取一项。 |
| `def add_cron_job(self, name: str, schedule: str, action: str) -> dict[str, Any]` | 1048 | 新增一条记录。 |
| `def delete_cron_job(self, job_id: str) -> bool` | 1064 | 删除一条记录。 |
| `def toggle_cron_job(self, job_id: str, enabled: bool) -> bool` | 1072 | `toggle_cron_job`：该模块的公开接口，参数见签名，行为见同行源码。 |
| `def get_evolution(self) -> dict[str, Any]` | 1093 | 按键或标识读取一项。 |
| `def run_evolution_now(self) -> dict[str, Any]` | 1122 | 执行主流程并返回结果。 |
| `def set_patch_status(self, patch_id: str, status: str) -> dict[str, Any]` | 1125 | 写入或更新一项。 |
| `def approve_skill(self, run_id: str, draft_id: str) -> dict[str, Any]` | 1135 | 技能草稿转正：实例化为正式工作流（cron 触发）。 |
| `def save_evolution_settings(self, enabled: bool, auto_apply_l01: bool, require_approval_l2: bool, cron: str = '') -> dict[str, Any]` | 1155 | 把当前对象写回本机配置文件。 |
| `def pick_attachments(self) -> list[dict[str, Any]]` | 1351 | 系统文件选择器 → 读取所有选中文件，返回附件描述列表。 |
| `def remove_attachment(self, index: int) -> list[dict[str, Any]]` | 1388 | `remove_attachment`：该模块的公开接口，参数见签名，行为见同行源码。 |
| `def copy_text(self, text: str) -> bool` | 1393 | 系统剪贴板写入（pbcopy / clip / xclip）。 |
| `def export_message(self, text: str) -> dict[str, Any]` | 1410 | 分享：把消息导出为 Markdown 文件（系统保存对话框）。 |
| `def get_projects(self) -> dict[str, Any]` | 1434 | 按键或标识读取一项。 |
| `def create_project(self, name: str, base: str = '', category: str = '其他') -> dict[str, Any]` | 1444 | 创建并返回新实例。 |
| `def get_project_records(self) -> dict[str, Any]` | 1454 | 当前项目的全部记录：对话 + 文件夹内文件。 |
| `def open_project_folder(self) -> dict[str, Any]` | 1466 | 在系统文件管理器中打开当前项目文件夹。 |
| `def switch_project(self, pid: str) -> dict[str, Any]` | 1486 | `switch_project`：该模块的公开接口，参数见签名，行为见同行源码。 |
| `def delete_project(self, pid: str) -> bool` | 1494 | 仅从索引移除；本地文件夹与对话记录全部保留。 |
| `def get_conversations(self) -> dict[str, Any]` | 1516 | 按键或标识读取一项。 |
| `def new_conversation(self) -> dict[str, Any]` | 1523 | `new_conversation`：该模块的公开接口，参数见签名，行为见同行源码。 |
| `def load_conversation(self, conv_id: str) -> dict[str, Any]` | 1528 | 从磁盘或默认路径加载持久化数据。 |
| `def send(self, text: str, resolution: str = '720p', num_frames: int = 60, num_inference_steps: int = 50) -> bool` | 1537 | 发送用户消息并驱动 Agent。 |
| `def stop(self) -> bool` | 1583 | Interrupt the in-flight chat (or lead) from the UI stop button. |
| `def reset(self) -> bool` | 1783 | `reset`：该模块的公开接口，参数见签名，行为见同行源码。 |
| `def lead(self, text: str) -> bool` | 1856 | `lead`：该模块的公开接口，参数见签名，行为见同行源码。 |
| `def get_runs(self) -> list[dict[str, Any]]` | 1922 | 按键或标识读取一项。 |
| `def get_memories(self, query: str = '') -> list[dict[str, Any]]` | 1939 | 按键或标识读取一项。 |
| `def add_memory(self, content: str) -> bool` | 1953 | 新增一条记录。 |
| `def delete_memory(self, memory_id: str) -> bool` | 1963 | 删除一条记录。 |
| `def get_knowledge(self) -> dict[str, Any]` | 1973 | 按键或标识读取一项。 |
| `def save_knowledge_config(self, path: str = '', mode: str = 'local', backend: str = 'obsidian', enabled: bool = True) -> dict[str, Any]` | 1994 | 把当前对象写回本机配置文件。 |
| `def bootstrap_knowledge(self) -> dict[str, Any]` | 2018 | One-click create Obsidian / LLM Wiki structure at configured path. |
| `def search_knowledge(self, query: str = '') -> list[dict[str, Any]]` | 2039 | `search_knowledge`：该模块的公开接口，参数见签名，行为见同行源码。 |
| `def read_knowledge_page(self, rel: str) -> dict[str, Any]` | 2054 | `read_knowledge_page`：该模块的公开接口，参数见签名，行为见同行源码。 |
| `def ingest_knowledge(self, title: str, content: str) -> dict[str, Any]` | 2061 | `ingest_knowledge`：该模块的公开接口，参数见签名，行为见同行源码。 |
| `def open_knowledge_folder(self) -> dict[str, Any]` | 2077 | `open_knowledge_folder`：该模块的公开接口，参数见签名，行为见同行源码。 |
| `def get_video_ops(self) -> dict[str, Any]` | 2101 | 按键或标识读取一项。 |
| `def save_video_ops_config(self, path: str = '', enabled: bool = True, gradio_base: str | None = None, comfy_base: str | None = None) -> dict[str, Any]` | 2156 | 把当前对象写回本机配置文件。 |
| `def bootstrap_video_ops(self) -> dict[str, Any]` | 2250 | `bootstrap_video_ops`：该模块的公开接口，参数见签名，行为见同行源码。 |
| `def save_video_ops_draft(self, title: str = '', description: str = '', tags: str = '', video_path: str = '', cover_path: str = '') -> dict[str, Any]` | 2277 | 把当前对象写回本机配置文件。 |
| `def get_studio(self) -> dict[str, Any]` | 2299 | 按键或标识读取一项。 |
| `def save_studio(self, payload: dict[str, Any] | None = None) -> dict[str, Any]` | 2337 | 把当前对象写回本机配置文件。 |
| `def studio_import_script(self, script: str) -> dict[str, Any]` | 2347 | `studio_import_script`：该模块的公开接口，参数见签名，行为见同行源码。 |
| `def studio_add_shot(self, title: str = '', prompt: str = '', seconds: float = 4.0, engine: str = 'auto') -> dict[str, Any]` | 2362 | `studio_add_shot`：该模块的公开接口，参数见签名，行为见同行源码。 |
| `def studio_remove_shot(self, shot_id: str) -> dict[str, Any]` | 2386 | `studio_remove_shot`：该模块的公开接口，参数见签名，行为见同行源码。 |
| `def studio_generate_shot(self, shot_id: str = '', all_pending: bool = False) -> dict[str, Any]` | 2398 | `studio_generate_shot`：该模块的公开接口，参数见签名，行为见同行源码。 |
| `def studio_assemble(self) -> dict[str, Any]` | 2455 | `studio_assemble`：该模块的公开接口，参数见签名，行为见同行源码。 |
| `def studio_interrupt(self) -> dict[str, Any]` | 2464 | `studio_interrupt`：该模块的公开接口，参数见签名，行为见同行源码。 |
| `def open_video_ops_folder(self) -> dict[str, Any]` | 2474 | `open_video_ops_folder`：该模块的公开接口，参数见签名，行为见同行源码。 |
| `def get_skills(self) -> list[dict[str, str]]` | 2498 | 按键或标识读取一项。 |
| `def get_harnesses(self) -> list[dict[str, Any]]` | 2512 | 按键或标识读取一项。 |
| `def get_logs(self, lines: int = 120) -> list[str]` | 2520 | 按键或标识读取一项。 |

## `src/codeagent/desktop/app.py`

> 模块说明：Desktop window launcher (pywebview). Import webview lazily so the package works without the ``desktop`` extra installed.

### `def run_desktop(root: Path | None = None) -> int`

- **定义行**：41
- **功能说明**：Open the codeagent desktop window. Blocks until the window closes.

## `src/codeagent/desktop/attachments.py`

> 模块说明：Chat attachment reading for the desktop composer. Every file type is recognized: text-like files are inlined directly, Office Open XML documents (.docx/.xlsx/.pptx) are extracted with the stdlib ``zipfile`` + XML stripping, PDFs use pypdf when installed, and anything else contributes its metadata so the model still knows the file exists and can reason about its name/type/size.

### `def read_attachment(path: Path) -> dict[str, Any]`

- **定义行**：78
- **功能说明**：Read one file and return {name, size, ext, kind, text, note}.

### `def format_attachments(items: list[dict[str, Any]]) -> str`

- **定义行**：126
- **功能说明**：Render attachments as a prompt prefix block.

## `src/codeagent/desktop/automation.py`

> 模块说明：Automation: workflow orchestration with continuous autonomy (LCA style). 一个工作流 = 触发方式 + 步骤数组 + 运行状态。步骤的 tool 字段是模型 引用（local:/api:/mix: 前缀）或留空走当前激活模型。执行是真实的： 每个步骤作为一次 agent 调用，上一步产出作为下一步上下文。 「连续性」开启后，结束后自动衔接下一轮，直至暂停或权限系统拦截。

### 类 `WorkflowStep`

- **基类**：`object`
- **定义行**：30
- **功能说明**：`WorkflowStep`：该模块的数据结构或服务类型，详见源码 docstring 与调用处。

### 类 `Workflow`

- **基类**：`object`
- **定义行**：36
- **功能说明**：`Workflow`：该模块的数据结构或服务类型，详见源码 docstring 与调用处。

### 类 `WorkflowStore`

- **基类**：`object`
- **定义行**：49
- **功能说明**：`WorkflowStore`：该模块的数据结构或服务类型，详见源码 docstring 与调用处。

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def load(cls, path: Path | None = None) -> 'WorkflowStore'` | 53 | 从磁盘或默认路径加载持久化数据。 |
| `def save(self, path: Path | None = None) -> None` | 66 | 把当前对象写回本机配置文件。 |
| `def get(self, wf_id: str) -> Workflow | None` | 77 | 按键或标识读取一项。 |

### 类 `WorkflowRunner`

- **基类**：`object`
- **定义行**：81
- **功能说明**：真实执行器：步骤链顺序调用 agent，连续性模式自动衔接。

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def __init__(self, store: WorkflowStore, run_step: Callable[[WorkflowStep, str], str], on_change: Callable[[Workflow], None] | None = None) -> None` | 84 | 构造并保存依赖（Provider、路径、配置等）。 |
| `def start(self, wf_id: str) -> bool` | 99 | `start`：该模块的公开接口，参数见签名，行为见同行源码。 |
| `def pause(self, wf_id: str) -> bool` | 143 | `pause`：该模块的公开接口，参数见签名，行为见同行源码。 |
| `def set_continuous(self, wf_id: str, on: bool) -> bool` | 153 | 写入或更新一项。 |

## `src/codeagent/desktop/brand.py`

> 模块说明：User-facing product name. Internal package/paths stay ``codeagent``.

（无公开类或函数；文件以常量、类型别名或再导出为主。）

## `src/codeagent/desktop/cron.py`

> 模块说明：Cron scheduling for the desktop app (LCA CronPage style, real scheduler). 五字段 cron 表达式（分 时 日 月 周），支持 * , - / 语法。调度线程每 30 秒对齐检查一次，到点即执行动作（当前动作 = 用指定/激活模型跑一次 agent 提示词），结果写回 lastRun/lastResult 并留活动流痕迹。

### 类 `CronJob`

- **基类**：`object`
- **定义行**：95
- **功能说明**：`CronJob`：该模块的数据结构或服务类型，详见源码 docstring 与调用处。

### 类 `CronStore`

- **基类**：`object`
- **定义行**：108
- **功能说明**：`CronStore`：该模块的数据结构或服务类型，详见源码 docstring 与调用处。

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def load(cls, path: Path | None = None) -> 'CronStore'` | 112 | 从磁盘或默认路径加载持久化数据。 |
| `def save(self, path: Path | None = None) -> None` | 122 | 把当前对象写回本机配置文件。 |

### 类 `CronScheduler`

- **基类**：`object`
- **定义行**：132
- **功能说明**：后台调度线程：每 30s 对齐检查，到点执行。

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def __init__(self, store: CronStore, run_action: Callable[[CronJob], bool], on_tick: Callable[[], None] | None = None, evolution_enabled: Callable[[], bool] | None = None, evolution_cron: Callable[[], str] | None = None, run_evolution: Callable[[], None] | None = None) -> None` | 135 | 构造并保存依赖（Provider、路径、配置等）。 |
| `def start(self) -> None` | 154 | `start`：该模块的公开接口，参数见签名，行为见同行源码。 |
| `def stop(self) -> None` | 160 | 请求停止当前生成。 |
| `def tick(self, now: float | None = None) -> list[str]` | 170 | Check all jobs once; returns fired job ids (testable). |

### `def cron_matches(expr: str, tm: time.struct_time) -> bool`

- **定义行**：60
- **功能说明**：5-field cron match against a local time (分 时 日 月 周, 周日=0/7).

### `def next_run_hint(expr: str) -> str`

- **定义行**：82
- **功能说明**：Forward-scan up to 7 days for the next matching minute.

## `src/codeagent/desktop/evolution.py`

> 模块说明：Evolution log: nightly evolution loop & behavior patches (LCA style). 五角色进化作业：复盘员重放当日交互、归因员定位低效维度、路由师微调 路由权重、记忆官压缩合并记忆、教官产出行为补丁与技能草稿。 行为补丁注入系统提示即生效；L0/L1 自动生效，L2 必须人工批准。

### 类 `BehaviorPatch`

- **基类**：`object`
- **定义行**：49
- **功能说明**：`BehaviorPatch`：该模块的数据结构或服务类型，详见源码 docstring 与调用处。

### 类 `EvolutionPhase`

- **基类**：`object`
- **定义行**：60
- **功能说明**：`EvolutionPhase`：该模块的数据结构或服务类型，详见源码 docstring 与调用处。

### 类 `SkillDraft`

- **基类**：`object`
- **定义行**：66
- **功能说明**：`SkillDraft`：该模块的数据结构或服务类型，详见源码 docstring 与调用处。

### 类 `EvolutionRun`

- **基类**：`object`
- **定义行**：75
- **功能说明**：`EvolutionRun`：该模块的数据结构或服务类型，详见源码 docstring 与调用处。

### 类 `EvolutionSettings`

- **基类**：`object`
- **定义行**：86
- **功能说明**：`EvolutionSettings`：该模块的数据结构或服务类型，详见源码 docstring 与调用处。

### 类 `EvolutionStore`

- **基类**：`object`
- **定义行**：94
- **功能说明**：`EvolutionStore`：该模块的数据结构或服务类型，详见源码 docstring 与调用处。

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def load(cls, path: Path | None = None) -> 'EvolutionStore'` | 100 | 从磁盘或默认路径加载持久化数据。 |
| `def save(self, path: Path | None = None) -> None` | 118 | 把当前对象写回本机配置文件。 |
| `def active_patch_rules(self) -> list[str]` | 133 | 生效中补丁的规则正文——注入系统提示。 |

### `def run_evolution(store: EvolutionStore, stats: dict[str, Any] | None = None, manual: bool = True) -> EvolutionRun`

- **定义行**：138
- **功能说明**：五阶段进化作业：复盘 → 归因 → 路由 → 记忆 → 教官。 ``stats`` 提供真实运行数据（对话数、事件数、反馈样本、记忆数）， 让阶段摘要言之有物。L0 且 auto_apply_l01 → 直接 active，否则 pending。

### `def set_patch(store: EvolutionStore, patch_id: str, status: str, verb: str) -> bool`

- **定义行**：203
- **功能说明**：补丁状态机唯一入口；回滚在审计语义上等同拒绝。

## `src/codeagent/desktop/models.py`

> 模块说明：Model assets for the desktop app (LunarCore Claw style). Two parallel kinds of callable models: - **local**: Ollama endpoints (LAN cluster); a model is ``model@endpoint`` - **api**: OpenAI-compatible services; key is optional (ds4/vLLM/llama.cpp need none) Security rules (mirroring LCA): - API keys never live in the asset store — only ``apikey:{id}`` pointers; the key body goes to ``~/.codeagent/secrets.json`` (chmod 600) - UI only ever shows masked keys (``••••••••`` + last 4) - Connectivity tests go through the *same* provider construction as real chat calls — a probe that takes a different path proves nothing

### 类 `OllamaEndpoint`

- **基类**：`object`
- **定义行**：85
- **功能说明**：`OllamaEndpoint`：该模块的数据结构或服务类型，详见源码 docstring 与调用处。

### 类 `Mixture`

- **基类**：`object`
- **定义行**：101
- **功能说明**：聚合池：本地 + API 成员混合路由（LCA Models.tsx 同款）.

### 类 `ApiModel`

- **基类**：`object`
- **定义行**：118
- **功能说明**：`ApiModel`：该模块的数据结构或服务类型，详见源码 docstring 与调用处。

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def secret_key(self) -> str` | 129 | `secret_key`：该模块的公开接口，参数见签名，行为见同行源码。 |
| `def display(self) -> str` | 133 | `display`：该模块的公开接口，参数见签名，行为见同行源码。 |

### 类 `ModelAssets`

- **基类**：`object`
- **定义行**：142
- **功能说明**：本地端点 + API 模型 + 聚合池。

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def load(cls, path: Path | None = None) -> 'ModelAssets'` | 151 | 从磁盘或默认路径加载持久化数据。 |
| `def save(self, path: Path | None = None) -> None` | 169 | 把当前对象写回本机配置文件。 |
| `def resolve_member(self, ref: str) -> tuple[str, dict[str, Any]] | None` | 188 | Any member ref → (kind, provider kwargs). |
| `def resolve_active(self) -> tuple[str, dict[str, Any]] | None` | 218 | Active asset → (kind, provider kwargs); None = legacy config. |
| `def member_label(self, ref: str) -> str` | 227 | Human label for a member ref (LCA modelName). |

### `def save_secret(key: str, value: str) -> None`

- **定义行**：54
- **功能说明**：把当前对象写回本机配置文件。

### `def load_secret(key: str) -> str`

- **定义行**：63
- **功能说明**：从磁盘或默认路径加载持久化数据。

### `def delete_secret(key: str) -> None`

- **定义行**：67
- **功能说明**：删除一条记录。

### `def mask_secret(value: str) -> str`

- **定义行**：76
- **功能说明**：`mask_secret`：该模块的公开接口，参数见签名，行为见同行源码。

### `def build_active_provider(assets: ModelAssets) -> LLMProvider | None`

- **定义行**：253
- **功能说明**：Construct the provider for the active asset (the *real* call path).

### `def normalize_endpoint_base(raw: str) -> str`

- **定义行**：279
- **功能说明**：Accept bare host:port / fullwidth colon / trailing slash.

### `async def probe_endpoint(base: str, timeout: float = 3.0) -> list[str]`

- **定义行**：291
- **功能说明**：List models: Ollama /api/tags, else OpenAI-compatible /v1/models.

### `async def probe_all(endpoints: list[OllamaEndpoint]) -> dict[str, Any]`

- **定义行**：299
- **功能说明**：Probe every endpoint concurrently; failures are visible, not fatal.

### `async def detect_service(base_url: str, api_key: str = '', timeout: float = 5.0) -> dict[str, Any]`

- **定义行**：334
- **功能说明**：Dual-protocol sniff: OpenAI /models first, then Ollama /api/tags. 大多数提供方（OpenAI/DeepSeek/Moonshot…）的 /models 需要鉴权， 必须带上用户填入的 API Key，否则一律 401 → 误判为"无法识别"。 鉴权失败且域名有已知孪生站点（如 Kimi 国内/国际）时自动换站重试。

### `async def test_provider(provider: LLMProvider, timeout: float = 45.0) -> dict[str, Any]`

- **定义行**：415
- **功能说明**：Real minimal call through the same path chat uses (hard timeout).

### `async def probe_endpoint_info(base: str, timeout: float = 3.0) -> dict[str, Any] | None`

- **定义行**：530
- **功能说明**：Probe → ``{kind, models}`` or None if offline. Prefer Ollama ``/api/tags``; then OpenAI ``/v1/models``; then Gradio ``/config``.

### `async def probe_endpoint_details(base: str, timeout: float = 3.0) -> list[dict[str, Any]] | None`

- **定义行**：586
- **功能说明**：Backward-compat: model detail list only (None if offline).

### `async def running_models(base: str, timeout: float = 3.0) -> set[str]`

- **定义行**：594
- **功能说明**：/api/ps → names currently loaded in memory (status=running).

### `async def set_model_loaded(base: str, model: str, load: bool, timeout: float = 120.0) -> None`

- **定义行**：605
- **功能说明**：Load (keep_alive=10m) or unload (keep_alive=0) a model in memory.

### `async def delete_ollama_model(base: str, model: str, timeout: float = 30.0) -> None`

- **定义行**：614
- **功能说明**：删除一条记录。

### `async def pull_ollama_model(base: str, model: str) -> None`

- **定义行**：622
- **功能说明**：Pull a model (long-running; caller runs it in a thread).

## `src/codeagent/desktop/permissions.py`

> 模块说明：Permission control for the desktop app (LunarCore Claw Permissions style). 能力矩阵：each capability maps to a set of agent tools and carries one of four levels — full（完全自主）/ confirm（执行前确认）/ readonly（只读）/ off（关闭）. Every non-trivial decision is written to the audit log.

### 类 `Confirmer`

- **基类**：`object`
- **定义行**：129
- **功能说明**：执行前确认：push a prompt to the UI, wait for the user's decision. 10 分钟未确认自动拒绝并记入审计（与 LCA 一致）。

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def __init__(self, push: Callable[[str, dict[str, Any]], None]) -> None` | 137 | 构造并保存依赖（Provider、路径、配置等）。 |
| `def ask(self, call: ToolCall, risk: RiskLevel) -> ApprovalDecision` | 143 | `ask`：该模块的公开接口，参数见签名，行为见同行源码。 |
| `def resolve(self, cid: str, approved: bool) -> bool` | 164 | `resolve`：该模块的公开接口，参数见签名，行为见同行源码。 |
| `def cancel_all(self) -> int` | 173 | Reject every pending confirm so a stopped run can unwind. |

### 类 `CapabilityPolicy`

- **基类**：`PermissionPolicy`
- **定义行**：183
- **功能说明**：Per-capability levels: confirm tools always ask (before auto-approve), denials are written to the audit chain.

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def __init__(self, confirm_tools: set[str], confirmer: Confirmer | None, **kwargs) -> None` | 187 | 构造并保存依赖（Provider、路径、配置等）。 |
| `async def authorize(self, call: ToolCall, risk: RiskLevel) -> ApprovalDecision` | 197 | `authorize`：该模块的公开接口，参数见签名，行为见同行源码。 |

### `def load_levels(path: Path | None = None) -> dict[str, str]`

- **定义行**：79
- **功能说明**：从磁盘或默认路径加载持久化数据。

### `def save_levels(levels: dict[str, str], path: Path | None = None) -> None`

- **定义行**：94
- **功能说明**：把当前对象写回本机配置文件。

### `def append_audit(actor: str, action: str, result: str, path: Path | None = None) -> None`

- **定义行**：100
- **功能说明**：Audit chain entry: actor（user/agent/system）· action · result.

### `def read_audit(limit: int = 12, path: Path | None = None) -> list[dict[str, Any]]`

- **定义行**：115
- **功能说明**：`read_audit`：该模块的公开接口，参数见签名，行为见同行源码。

### `def build_policy(levels: dict[str, str], confirmer: Confirmer | None) -> PermissionPolicy`

- **定义行**：206
- **功能说明**：Levels → PermissionPolicy (fail closed for unknown tools).

## `src/codeagent/desktop/privacy.py`

> 模块说明：Privacy policy text for CodeCoreAgent (desktop). ``PRIVACY_VERSION`` bumps when material terms change; the UI re-prompts until the user accepts the new version (stored in ``desktop.json``).

### `def privacy_document() -> dict[str, Any]`

- **定义行**：85
- **功能说明**：Structured policy for the desktop UI / API.

### `def privacy_markdown() -> str`

- **定义行**：95
- **功能说明**：Plain markdown mirror (also written to PRIVACY.md).

## `src/codeagent/desktop/projects.py`

> 模块说明：Project workspaces for the desktop app. 每个项目对应一个本地文件夹，agent 的工作根目录即项目文件夹—— 所有产出文件、对话记录都在文件夹里： <项目文件夹>/ ├── project.json # 元数据 ├── files/ # 上传附件与用户放入的文件 └── conversations/ ├── <id>.jsonl # 对话事件流（程序用） └── <id>.md # 可读对话记录（人看） 项目按分类（工作 / 个人 / 学习 / 其他）组织。 项目索引存于 ~/.codeagent/projects.json。

### 类 `Project`

- **基类**：`object`
- **定义行**：34
- **功能说明**：`Project`：该模块的数据结构或服务类型，详见源码 docstring 与调用处。

### 类 `ProjectStore`

- **基类**：`object`
- **定义行**：44
- **功能说明**：`ProjectStore`：该模块的数据结构或服务类型，详见源码 docstring 与调用处。

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def load(cls, path: Path | None = None) -> 'ProjectStore'` | 49 | 从磁盘或默认路径加载持久化数据。 |
| `def save(self, path: Path | None = None) -> None` | 64 | 把当前对象写回本机配置文件。 |
| `def get(self, pid: str) -> Project | None` | 70 | 按键或标识读取一项。 |
| `def create(self, name: str, base: str = '', category: str = '其他') -> Project` | 73 | 建立项目：同时在本地创建项目文件夹（含 conversations/ 与 files/）。 |
| `def ensure_default(self) -> Project` | 92 | 没有项目时自动建立一个默认项目，保证对话不会丢。 |
| `def remove(self, pid: str) -> bool` | 101 | 仅从索引移除（本地文件夹与对话记录保留）。 |
| `def touch(self, pid: str) -> None` | 112 | `touch`：该模块的公开接口，参数见签名，行为见同行源码。 |

### `def conversation_dir(project: Project) -> Path`

- **定义行**：134
- **功能说明**：`conversation_dir`：该模块的公开接口，参数见签名，行为见同行源码。

### `def new_conversation_id() -> str`

- **定义行**：140
- **功能说明**：`new_conversation_id`：该模块的公开接口，参数见签名，行为见同行源码。

### `def append_message(project: Project, conv_id: str, role: str, text: str) -> None`

- **定义行**：144
- **功能说明**：追加一条消息到对话（jsonl 事件流 + 同步重写 md 可读版）。

### `def load_conversation(project: Project, conv_id: str) -> list[dict[str, Any]]`

- **定义行**：153
- **功能说明**：从磁盘或默认路径加载持久化数据。

### `def list_conversations(project: Project) -> list[dict[str, Any]]`

- **定义行**：166
- **功能说明**：按时间倒序列出项目全部对话（含首条用户消息作标题）。

### `def files_dir(project: Project) -> Path`

- **定义行**：198
- **功能说明**：`files_dir`：该模块的公开接口，参数见签名，行为见同行源码。

### `def ingest_file(project: Project, src: Path) -> Path`

- **定义行**：204
- **功能说明**：把外部文件复制进项目 files/，重名自动加序号。

### `def list_files(project: Project) -> list[dict[str, Any]]`

- **定义行**：216
- **功能说明**：列出项目文件夹内的用户文件（不含对话记录与元数据）。

## `src/codeagent/desktop/router.py`

> 模块说明：Routing engine for the desktop app (LunarCore Claw RouterPage style). 规则 → 分类 → 级联 → 学习：rules are matched by keyword against the incoming message; the first hit (by priority) picks the target — a mixture, an API model, or a local model. The sandbox previews the decision without calling any model.

### 类 `RouteRule`

- **基类**：`object`
- **定义行**：48
- **功能说明**：`RouteRule`：该模块的数据结构或服务类型，详见源码 docstring 与调用处。

### 类 `RouteWeights`

- **基类**：`object`
- **定义行**：58
- **功能说明**：`RouteWeights`：该模块的数据结构或服务类型，详见源码 docstring 与调用处。

### 类 `RouterStore`

- **基类**：`object`
- **定义行**：65
- **功能说明**：自由路由规则与权重。

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def load(cls, path: Path | None = None) -> 'RouterStore'` | 70 | 从磁盘或默认路径加载持久化数据。 |
| `def save(self, path: Path | None = None) -> None` | 83 | 把当前对象写回本机配置文件。 |
| `def sorted_rules(self) -> list[RouteRule]` | 96 | `sorted_rules`：该模块的公开接口，参数见签名，行为见同行源码。 |

### `def is_free_route(ref: str) -> bool`

- **定义行**：25
- **功能说明**：Empty (legacy) and ``route:free`` both mean: let the routing engine decide.

### `def split_keywords(raw: str) -> list[str]`

- **定义行**：30
- **功能说明**：Split rule keywords on ASCII/Chinese commas and enumeration pauses.

### `def classify_task(text: str) -> str`

- **定义行**：100
- **功能说明**：Built-in classifier: keyword hit → task type; else 通用对话.

### `def route_message(text: str, store: RouterStore, resolve_label) -> dict[str, Any]`

- **定义行**：109
- **功能说明**：Preview a routing decision without calling any model. ``resolve_label(ref)`` maps a target ref to its display name. Returns the LCA-shaped decision: taskType / strategy / reason / candidates / chosen / latencyMs / cost.

## `src/codeagent/desktop/ui.py`

> 模块说明：Desktop UI: single-page HTML/CSS/JS loaded into the webview window. Design language follows LunarCore Claw (LCA): shadcn-style zinc dark theme, grouped left sidebar, white primary accent, 10px radius, lucide-style icons.

（无公开类或函数；文件以常量、类型别名或再导出为主。）

## `src/codeagent/eval/__init__.py`

（无公开类或函数；文件以常量、类型别名或再导出为主。）

## `src/codeagent/eval/harness.py`

> 模块说明：Evaluation harness: measure the agent on code-generation benchmarks. Tasks carry a prompt plus test code; the agent's answer is mined for a code block, combined with the tests, and executed. pass@1 is the headline metric, matching how MBPP / HumanEval-style benchmarks are reported. Note: verification executes generated code. Run evaluations inside a sandbox or container.

### 类 `EvalTask`

- **基类**：`object`
- **定义行**：35
- **功能说明**：One benchmark item.

### 类 `EvalResult`

- **基类**：`object`
- **定义行**：45
- **功能说明**：`EvalResult`：该模块的数据结构或服务类型，详见源码 docstring 与调用处。

### 类 `EvalReport`

- **基类**：`object`
- **定义行**：54
- **功能说明**：`EvalReport`：该模块的数据结构或服务类型，详见源码 docstring 与调用处。

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def pass_at_1(self) -> float` | 58 | `pass_at_1`：该模块的公开接口，参数见签名，行为见同行源码。 |
| `def summary(self) -> str` | 64 | `summary`：该模块的公开接口，参数见签名，行为见同行源码。 |

### `def extract_code(answer: str) -> str`

- **定义行**：26
- **功能说明**：Pull the last fenced code block out of an answer; fall back to raw text.

### `async def evaluate(agent_factory: AgentFactory, tasks: list[EvalTask], max_concurrency: int = 4) -> EvalReport`

- **定义行**：124
- **功能说明**：Run every task against a fresh agent and score the answers.

## `src/codeagent/eval/mbpp.py`

> 模块说明：Loader for MBPP — the Mostly Basic Python Problems benchmark. MBPP lives in google-research/google-research (CC BY 4.0). Each row has: ``task_id``, ``text`` (problem statement), ``code`` (reference solution), ``test_list`` (assert statements) and optional ``test_setup_code``.

### `def load_mbpp(path: str | Path | None = None, limit: int | None = None, sanitized: bool = False) -> list[EvalTask]`

- **定义行**：43
- **功能说明**：Load MBPP tasks from a local jsonl file, or download from GitHub. ``sanitized=True`` loads the human-verified subset file instead.

## `src/codeagent/harness/__init__.py`

> 模块说明：External agent platform adapters: call other CLI agents when stuck.

（无公开类或函数；文件以常量、类型别名或再导出为主。）

## `src/codeagent/harness/adapters.py`

> 模块说明：External agent platform adapters (HiveWard's "harness employees"). When a worker is stuck, confused, or fails outright, it can hand the task to another agent platform installed on this computer — Claude Code, Codex, Gemini CLI, cursor-agent, opencode — via its CLI, in headless mode.

### 类 `HarnessResult`

- **基类**：`object`
- **定义行**：17
- **功能说明**：`HarnessResult`：该模块的数据结构或服务类型，详见源码 docstring 与调用处。

### 类 `CliHarness`

- **基类**：`object`
- **定义行**：25
- **功能说明**：A headless CLI agent platform. ``argv`` contains a {prompt} slot.

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def executable(self) -> str` | 34 | `executable`：该模块的公开接口，参数见签名，行为见同行源码。 |
| `def available(self) -> bool` | 37 | `available`：该模块的公开接口，参数见签名，行为见同行源码。 |
| `async def run(self, prompt: str, cwd: str | Path | None = None) -> HarnessResult` | 40 | 执行主流程并返回结果。 |

### `def discover_harnesses(extra: list[CliHarness] | None = None) -> list[CliHarness]`

- **定义行**：92
- **功能说明**：Return the agent platforms actually installed on this machine.

## `src/codeagent/harness/tool.py`

> 模块说明：ask_external_agent: a tool letting a stuck worker call another agent platform.

### 类 `AskHarnessTool`

- **基类**：`Tool`
- **定义行**：13
- **功能说明**：`AskHarnessTool`：该模块的数据结构或服务类型，详见源码 docstring 与调用处。

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def __init__(self, harnesses: list[CliHarness] | None = None, cwd: str | Path | None = None) -> None` | 36 | 构造并保存依赖（Provider、路径、配置等）。 |
| `async def execute(self, prompt: str, harness: str | None = None) -> str` | 44 | 真正执行工具副作用并返回文本。 |

## `src/codeagent/knowledge/__init__.py`

> 模块说明：CodeCoreAgent knowledge base — Obsidian / LLM-Wiki vault. Layout (Karpathy LLM Wiki pattern, Obsidian-compatible):: <vault>/ AGENTS.md # schema for CodeCoreAgent / any agent .obsidian/ # minimal so Obsidian opens the folder as a vault raw/ # Layer 1 — immutable sources inbox/ projects/ conversations/ wiki/ # Layer 2 — LLM-maintained pages index.md log.md overview.md concepts/ entities/ projects/ syntheses/ Connection modes: - **local** — path on this machine (default ``~/Documents/CodeCoreAgent-Wiki``) - **shared** — network / synced folder (NAS mount, SMB, Syncthing, iCloud, Dropbox, OneDrive, etc.). Same vault layout; any computer pointing at the same path shares the knowledge base.

### 类 `KnowledgeConfig`

- **基类**：`object`
- **定义行**：165
- **功能说明**：Persisted connection settings for the knowledge vault.

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def load(cls, path: Path | None = None) -> 'KnowledgeConfig'` | 185 | 从磁盘或默认路径加载持久化数据。 |
| `def save(self, path: Path | None = None) -> None` | 196 | 把当前对象写回本机配置文件。 |
| `def vault_path(self) -> Path` | 204 | `vault_path`：该模块的公开接口，参数见签名，行为见同行源码。 |

### 类 `WikiPage`

- **基类**：`object`
- **定义行**：209
- **功能说明**：`WikiPage`：该模块的数据结构或服务类型，详见源码 docstring 与调用处。

### `def is_vault_ready(root: Path) -> bool`

- **定义行**：225
- **功能说明**：`is_vault_ready`：该模块的公开接口，参数见签名，行为见同行源码。

### `def bootstrap_vault(root: Path) -> dict[str, Any]`

- **定义行**：229
- **功能说明**：Create the CodeCoreAgent LLM Wiki / Obsidian layout. Idempotent.

### `def vault_status(root: Path) -> dict[str, Any]`

- **定义行**：267
- **功能说明**：`vault_status`：该模块的公开接口，参数见签名，行为见同行源码。

### `def list_pages(root: Path, query: str = '', limit: int = 80) -> list[WikiPage]`

- **定义行**：291
- **功能说明**：列出集合中的条目。

### `def read_page(root: Path, rel: str) -> dict[str, Any] | None`

- **定义行**：333
- **功能说明**：`read_page`：该模块的公开接口，参数见签名，行为见同行源码。

### `def append_log(root: Path, event: str) -> None`

- **定义行**：354
- **功能说明**：`append_log`：该模块的公开接口，参数见签名，行为见同行源码。

### `def ingest_text(root: Path, title: str, body: str, folder: str = 'raw/inbox') -> dict[str, Any]`

- **定义行**：365
- **功能说明**：Drop a markdown note into raw/ (immutable source layer).

## `src/codeagent/knowledge/tools.py`

> 模块说明：Agent tools for the CodeCoreAgent knowledge vault (Obsidian / LLM Wiki).

### 类 `KnowledgeSearchTool`

- **基类**：`Tool`
- **定义行**：19
- **功能说明**：`KnowledgeSearchTool`：该模块的数据结构或服务类型，详见源码 docstring 与调用处。

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def __init__(self, root: Path) -> None` | 39 | 构造并保存依赖（Provider、路径、配置等）。 |
| `async def execute(self, query: str, limit: int = 8, **_) -> str` | 42 | 真正执行工具副作用并返回文本。 |

### 类 `KnowledgeReadTool`

- **基类**：`Tool`
- **定义行**：50
- **功能说明**：`KnowledgeReadTool`：该模块的数据结构或服务类型，详见源码 docstring 与调用处。

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def __init__(self, root: Path) -> None` | 64 | 构造并保存依赖（Provider、路径、配置等）。 |
| `async def execute(self, path: str, **_) -> str` | 67 | 真正执行工具副作用并返回文本。 |

### 类 `KnowledgeIngestTool`

- **基类**：`Tool`
- **定义行**：77
- **功能说明**：`KnowledgeIngestTool`：该模块的数据结构或服务类型，详见源码 docstring 与调用处。

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def __init__(self, root: Path) -> None` | 93 | 构造并保存依赖（Provider、路径、配置等）。 |
| `async def execute(self, title: str, content: str, **_) -> str` | 96 | 真正执行工具副作用并返回文本。 |

### `def knowledge_tools(cfg: KnowledgeConfig | None = None) -> list[Tool]`

- **定义行**：101
- **功能说明**：Return tools if vault is configured and ready; else empty list.

## `src/codeagent/leader/__init__.py`

> 模块说明：Leader: voice/text command seat orchestrating multiple worker models.

（无公开类或函数；文件以常量、类型别名或再导出为主。）

## `src/codeagent/leader/archive.py`

> 模块说明：Run archive: every leader command is persisted locally, per project. All records stay on this machine under ``~/.codeagent/runs/<project>/`` — progress history survives restarts, and every run is replayable/auditable.

### 类 `RunRecord`

- **基类**：`object`
- **定义行**：28
- **功能说明**：`RunRecord`：该模块的数据结构或服务类型，详见源码 docstring 与调用处。

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def to_dict(self) -> dict[str, Any]` | 36 | `to_dict`：该模块的公开接口，参数见签名，行为见同行源码。 |

### 类 `RunArchive`

- **基类**：`object`
- **定义行**：47
- **功能说明**：JSON-file archive of leader runs, stored locally per project.

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def __init__(self, base: str | Path = DEFAULT_ARCHIVE_ROOT) -> None` | 50 | 构造并保存依赖（Provider、路径、配置等）。 |
| `def save(self, project_root: str | Path, command: str, reply: str, board_snapshot: dict[str, Any]) -> Path` | 58 | 把当前对象写回本机配置文件。 |
| `def list(self, project_root: str | Path | None = None) -> list[dict[str, Any]]` | 84 | All archived runs (optionally for one project), newest first. |

### `def project_key(root: str | Path) -> str`

- **定义行**：20
- **功能说明**：Stable per-project key: readable name + short path hash.

## `src/codeagent/leader/leader.py`

> 模块说明：Leader: receives voice/text commands, plans, dispatches to worker models. Fuses HomeRail's ideas into a single seat: - one leader, many worker models (smart brain, efficient workers) - workers may collaborate on one task or run several tasks in parallel - progress board is queryable at any moment (voice readout included) - every worker understands the project and can use all skills/tools

### 类 `Leader`

- **基类**：`object`
- **定义行**：83
- **功能说明**：The boss seat: command in, coordinated work out, progress anytime.

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def __init__(self, provider: LLMProvider, workers: list[WorkerConfig], root: str | Path = '.', skills: SkillLibrary | None = None, registry_factory: Callable[[], ToolRegistry] | None = None, board: ProgressBoard | None = None, budget: Budget | None = None, max_iterations: int = 30, harnesses: list[CliHarness] | None = None, escalate: bool = True, archive: RunArchive | None = None, settings: 'Settings | None' = None, on_event: Callable[[str, Any], None] | None = None, worker_retries: int = 2, max_parallel: int | None = None) -> None` | 86 | 构造并保存依赖（Provider、路径、配置等）。 |
| `async def command(self, text: str) -> str` | 133 | Handle one boss command (from text or voice transcription). |

### `def project_context(root: Path, max_entries: int = 40, readme_chars: int = 1200) -> str`

- **定义行**：63
- **功能说明**：A compact project brief so every worker understands the workspace.

## `src/codeagent/leader/progress.py`

> 模块说明：Progress board: live, queryable view of who is doing what (HomeRail supervise).

### 类 `TaskRecord`

- **基类**：`object`
- **定义行**：20
- **功能说明**：`TaskRecord`：该模块的数据结构或服务类型，详见源码 docstring 与调用处。

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def elapsed(self) -> float` | 30 | `elapsed`：该模块的公开接口，参数见签名，行为见同行源码。 |

### 类 `ProgressBoard`

- **基类**：`object`
- **定义行**：34
- **功能说明**：Tracks every dispatched task; safe to query while work is running.

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def __init__(self, on_change: Callable[[TaskRecord], None] | None = None) -> None` | 37 | 构造并保存依赖（Provider、路径、配置等）。 |
| `def register(self, task_id: str, title: str, worker: str) -> TaskRecord` | 42 | `register`：该模块的公开接口，参数见签名，行为见同行源码。 |
| `def update(self, task_id: str, status: TaskStatus, detail: str = '') -> None` | 49 | `update`：该模块的公开接口，参数见签名，行为见同行源码。 |
| `def get(self, task_id: str) -> TaskRecord | None` | 58 | 按键或标识读取一项。 |
| `def records(self) -> list[TaskRecord]` | 61 | `records`：该模块的公开接口，参数见签名，行为见同行源码。 |
| `def snapshot(self) -> dict[str, Any]` | 64 | `snapshot`：该模块的公开接口，参数见签名，行为见同行源码。 |
| `def summary(self) -> str` | 83 | Voice-friendly Chinese progress readout. |
| `def clear(self) -> None` | 100 | `clear`：该模块的公开接口，参数见签名，行为见同行源码。 |

## `src/codeagent/leader/worker.py`

> 模块说明：Worker: a project-aware agent instance bound to its own model.

### 类 `WorkerConfig`

- **基类**：`object`
- **定义行**：22
- **功能说明**：One worker's identity: which model it runs and what it's good at.

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def from_dict(cls, data: dict[str, Any]) -> 'WorkerConfig'` | 33 | `from_dict`：该模块的公开接口，参数见签名，行为见同行源码。 |

### `def load_workers_yaml(path: str | Path) -> list[WorkerConfig]`

- **定义行**：49
- **功能说明**：Load worker roster from YAML: ``workers: [{name, provider, ...}]``.

### `def build_worker_agent(config: WorkerConfig, root: Path, registry: ToolRegistry | None = None, skills: SkillLibrary | None = None, settings: 'Settings | None' = None, project_context: str = '', max_iterations: int = 30, budget: Budget | None = None, on_event = None) -> Agent`

- **定义行**：61
- **功能说明**：Build a worker agent with full tools, skills, and project understanding.

## `src/codeagent/llm/__init__.py`

（无公开类或函数；文件以常量、类型别名或再导出为主。）

## `src/codeagent/llm/aggregate.py`

> 模块说明：Aggregate provider: combine multiple LLM backends into one. Typical mixes: - local-first: ``ollama`` primary, cloud API as backup (free when possible) - failover: several API keys/relays, automatically skip the broken one - round-robin: spread load across providers

### 类 `AggregateError`

- **基类**：`Exception`
- **定义行**：20
- **功能说明**：Raised when every sub-provider failed.

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def __init__(self, errors: list[tuple[str, Exception]]) -> None` | 23 | 构造并保存依赖（Provider、路径、配置等）。 |

### 类 `AggregateProvider`

- **基类**：`LLMProvider`
- **定义行**：29
- **功能说明**：Meta-provider routing calls across several providers. ``strategy="fallback"`` tries providers in order and moves on when one raises; ``strategy="round-robin"`` rotates the starting provider on every call while still failing over around the ring.

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def __init__(self, providers: list[LLMProvider], strategy: Strategy = 'fallback', max_tokens: int = 8192) -> None` | 39 | 构造并保存依赖（Provider、路径、配置等）。 |
| `async def complete(self, messages: list[Message], tools: list[dict] | None = None, system: str | None = None, **kwargs) -> LLMResponse` | 67 | 向模型请求一轮补全。 |

### `def parse_provider_spec(spec: str, strategy: Strategy = 'fallback', **kwargs) -> LLMProvider`

- **定义行**：86
- **功能说明**：Build a provider from a spec like ``"ollama,openrouter,anthropic"``. A single name returns a plain provider; a comma-separated list returns an :class:`AggregateProvider` over them. Keyword arguments (``model``, ``api_key``, ``base_url``) are forwarded to every sub-provider — prefer per-service environment variables when mixing services.

## `src/codeagent/llm/anthropic.py`

> 模块说明：Anthropic Claude provider.

### 类 `AnthropicProvider`

- **基类**：`LLMProvider`
- **定义行**：14
- **功能说明**：Claude Messages API。

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def __init__(self, model: str = DEFAULT_MODEL, api_key: str | None = None, max_tokens: int = 8192, **client_kwargs) -> None` | 17 | 构造并保存依赖（Provider、路径、配置等）。 |
| `async def complete(self, messages: list[Message], tools: list[dict] | None = None, system: str | None = None, **kwargs) -> LLMResponse` | 37 | 向模型请求一轮补全。 |

## `src/codeagent/llm/base.py`

> 模块说明：Abstract interface every LLM provider implements.

### 类 `LLMProvider`

- **基类**：`ABC`
- **定义行**：10
- **功能说明**：A chat-completion backend with optional tool use. Providers consume the framework's unified :class:`Message` history and generic JSON-Schema tool definitions, and return a normalized :class:`LLMResponse`. All provider-specific wire formats are translated inside the provider implementation.

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def __init__(self, model: str, max_tokens: int = 8192) -> None` | 21 | 构造并保存依赖（Provider、路径、配置等）。 |
| `async def complete(self, messages: list[Message], tools: list[dict] | None = None, system: str | None = None, **kwargs) -> LLMResponse` | 26 | Produce the next assistant turn. ``tools`` is a list of ``{"name", "description", "parameters"}`` dicts where ``parameters`` is a JSON Schema object. |

## `src/codeagent/llm/ollama.py`

> 模块说明：Ollama provider: native /api/chat (streaming NDJSON), not the OpenAI shim. Why native: Ollama's OpenAI-compatible endpoint silently ignores the ``think`` switch, so thinking models (qwen3, glm-4.7…) burn the whole num_predict budget on reasoning and return empty content. The native API takes ``think`` as a *top-level* field (inside ``options`` it is silently ignored — same lesson LunarCore Claw learned in v0.17.4).

### 类 `OllamaProvider`

- **基类**：`LLMProvider`
- **定义行**：50
- **功能说明**：Local Ollama model via the native chat API. ``think=False`` (default) disables the reasoning stream of thinking models — agent loops and one-shot machine calls want the answer, not the chain-of-thought eating the token budget.

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def __init__(self, model: str = DEFAULT_MODEL, base_url: str = DEFAULT_BASE_URL, api_key: str | None = None, max_tokens: int = 8192, think: bool = False, timeout: float = 300.0, **_) -> None` | 60 | 构造并保存依赖（Provider、路径、配置等）。 |
| `async def complete(self, messages: list[Message], tools: list[dict] | None = None, system: str | None = None, **kwargs) -> LLMResponse` | 78 | 向模型请求一轮补全。 |

## `src/codeagent/llm/openai.py`

> 模块说明：OpenAI provider (also covers OpenAI-compatible endpoints).

### 类 `LLMError`

- **基类**：`RuntimeError`
- **定义行**：51
- **功能说明**：Provider call succeeded at HTTP level but yielded no usable answer.

### 类 `OpenAIProvider`

- **基类**：`LLMProvider`
- **定义行**：55
- **功能说明**：OpenAI 及兼容端点（含推理字段兜底）。

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def __init__(self, model: str = DEFAULT_MODEL, api_key: str | None = None, base_url: str | None = None, max_tokens: int = 8192, **client_kwargs) -> None` | 58 | 构造并保存依赖（Provider、路径、配置等）。 |
| `async def complete(self, messages: list[Message], tools: list[dict] | None = None, system: str | None = None, **kwargs) -> LLMResponse` | 84 | 向模型请求一轮补全。 |

### `def is_transient_serving_error(exc: BaseException) -> bool`

- **定义行**：28
- **功能说明**：True for retryable cloud 5xx / DashScope InternalError.Algo.

## `src/codeagent/llm/presets.py`

> 模块说明：Provider presets: aggregation gateways and local deployment servers. Aggregation gateways expose many upstream models behind one OpenAI-compatible endpoint (one key, every model). Local servers run models on this machine, key-free.

（无公开类或函数；文件以常量、类型别名或再导出为主。）

## `src/codeagent/llm/registry.py`

> 模块说明：Provider registry: construct LLM backends from string names.

### `def register_provider(name: str, provider_cls: type[LLMProvider]) -> None`

- **定义行**：21
- **功能说明**：Register a custom provider class under ``name``.

### `def list_providers() -> list[str]`

- **定义行**：28
- **功能说明**：列出集合中的条目。

### `def create_provider(name: str, **kwargs) -> LLMProvider`

- **定义行**：32
- **功能说明**：Instantiate a provider by name. Extra keyword arguments (``model``, ``api_key``, ``base_url``, ...) are forwarded to the provider constructor.

## `src/codeagent/log.py`

> 模块说明：Persistent run logging: every session appended to ~/.codeagent/logs/. Uses a rotating file handler (1 MB × 5 backups). Level controlled by the ``CODEAGENT_LOG_LEVEL`` env var (default INFO). Library users call ``setup_logging()`` once; the CLI does it automatically per command.

### `def setup_logging(level: str | int | None = None, log_file: Path | None = None) -> Path`

- **定义行**：23
- **功能说明**：Attach a rotating file handler to the ``codeagent`` logger tree. Idempotent — repeated calls reuse the first configuration. Returns the active log file path.

### `def get_logger(name: str) -> logging.Logger`

- **定义行**：54
- **功能说明**：Child logger under the ``codeagent`` tree (e.g. ``codeagent.agent``).

### `def tail_log(lines: int = 50, log_file: Path | None = None) -> list[str]`

- **定义行**：59
- **功能说明**：Return the last ``lines`` lines of the active log file.

## `src/codeagent/mcp/__init__.py`

（无公开类或函数；文件以常量、类型别名或再导出为主。）

## `src/codeagent/mcp/client.py`

> 模块说明：MCP (Model Context Protocol) integration: load tools from MCP servers. Server configs use the de-facto standard Claude-style JSON format, with either a stdio ``command`` or a streamable-HTTP ``url``:: { "mcpServers": { "filesystem": { "command": "npx", "args": ["-y", "@modelcontextprotocol/server-filesystem", "/tmp"] }, "keenable": { "type": "streamable-http", "url": "https://api.keenable.ai/mcp", "headers": {"X-API-Key": "..."} } } }

### 类 `MCPServerConfig`

- **基类**：`object`
- **定义行**：34
- **功能说明**：Connection settings for one MCP server. Stdio transport: set ``command`` (plus optional ``args`` / ``env``). Streamable-HTTP transport: set ``url`` (plus optional ``headers``).

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def transport(self) -> str` | 49 | `transport`：该模块的公开接口，参数见签名，行为见同行源码。 |

### 类 `MCPTool`

- **基类**：`Tool`
- **定义行**：91
- **功能说明**：A codeagent tool proxied to a tool on a remote MCP server.

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def __init__(self, session: Any, server_name: str, remote_name: str, description: str, input_schema: dict[str, Any] | None) -> None` | 96 | 构造并保存依赖（Provider、路径、配置等）。 |
| `async def execute(self, **arguments) -> str` | 111 | 真正执行工具副作用并返回文本。 |

### 类 `MCPManager`

- **基类**：`object`
- **定义行**：123
- **功能说明**：Connects to MCP servers and exposes their tools as codeagent tools. Supports stdio and streamable-HTTP transports. Usage:: async with MCPManager(configs) as mcp: registry = default_tools(".") for tool in mcp.tools(): registry.register(tool) agent = Agent(provider=..., tools=registry)

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def __init__(self, configs: list[MCPServerConfig]) -> None` | 135 | 构造并保存依赖（Provider、路径、配置等）。 |
| `async def connect(self) -> MCPManager` | 140 | 建立网络或设备连接。 |
| `def tools(self) -> list[MCPTool]` | 196 | `tools`：该模块的公开接口，参数见签名，行为见同行源码。 |
| `async def close(self) -> None` | 199 | `close`：该模块的公开接口，参数见签名，行为见同行源码。 |
| `async def __aenter__(self) -> MCPManager` | 205 | `__aenter__`：该模块的公开接口，参数见签名，行为见同行源码。 |
| `async def __aexit__(self, *exc_info) -> None` | 208 | `__aexit__`：该模块的公开接口，参数见签名，行为见同行源码。 |

### `def load_mcp_config(path: str | Path) -> list[MCPServerConfig]`

- **定义行**：59
- **功能说明**：Parse a Claude-style MCP config file into server configs.

## `src/codeagent/mcp/presets.py`

> 模块说明：Ready-made MCP server presets.

### `def keenable(api_key: str | None = None) -> MCPServerConfig`

- **定义行**：10
- **功能说明**：Keenable web search + clean-markdown page fetch (hosted MCP server). Provides the ``search_web_pages`` and ``fetch_page_content`` tools. Keyless by default (1,000 requests/hour); pass ``api_key`` to lift the rate limit.

### `def playwright_mcp(headless: bool = True, browser: str = 'chromium') -> MCPServerConfig`

- **定义行**：21
- **功能说明**：Microsoft Playwright MCP: full browser automation for the agent. Requires Node.js; the server is launched via ``npx @playwright/mcp``. Provides tools like ``browser_navigate``, ``browser_click``, ``browser_type``, ``browser_take_screenshot`` etc.

### `def chrome_devtools_mcp(headless: bool = True) -> MCPServerConfig`

- **定义行**：34
- **功能说明**：Chrome DevTools MCP: inspect, debug and profile pages in Chrome. Requires Node.js; the server is launched via ``npx chrome-devtools-mcp``. Provides performance tracing, network inspection, console messages, DOM snapshots and more.

## `src/codeagent/memory/__init__.py`

（无公开类或函数；文件以常量、类型别名或再导出为主。）

## `src/codeagent/memory/compact.py`

> 模块说明：Short-term memory: conversation compaction. When the message history grows past a threshold, older turns are summarized by the model itself and replaced with a compact summary, keeping the most recent turns verbatim. This mirrors the "local summary" short-term memory pattern (and Codex's compaction): unbounded sessions without unbounded context growth.

### 类 `CompactionConfig`

- **基类**：`object`
- **定义行**：36
- **功能说明**：`CompactionConfig`：该模块的数据结构或服务类型，详见源码 docstring 与调用处。

### 类 `ConversationCompactor`

- **基类**：`object`
- **定义行**：41
- **功能说明**：Summarizes old conversation turns using an LLM provider.

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def __init__(self, provider: LLMProvider, config: CompactionConfig | None = None) -> None` | 44 | 构造并保存依赖（Provider、路径、配置等）。 |
| `async def maybe_compact(self, messages: list[Message]) -> list[Message]` | 52 | `maybe_compact`：该模块的公开接口，参数见签名，行为见同行源码。 |

## `src/codeagent/memory/facts.py`

> 模块说明：Phase 1 of mem0-style memory writing: extract atomic facts with an LLM. Given raw text (a conversation snippet, a user statement), the model pulls out self-contained atomic facts — e.g. "名字叫张三", "李四在小米工作" — instead of storing the raw text verbatim.

### 类 `FactExtractor`

- **基类**：`object`
- **定义行**：43
- **功能说明**：`FactExtractor`：该模块的数据结构或服务类型，详见源码 docstring 与调用处。

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def __init__(self, provider: LLMProvider) -> None` | 44 | 构造并保存依赖（Provider、路径、配置等）。 |
| `async def extract(self, text: str) -> list[str]` | 47 | `extract`：该模块的公开接口，参数见签名，行为见同行源码。 |

### `def parse_json_object(text: str) -> dict`

- **定义行**：31
- **功能说明**：Leniently parse the first JSON object in an LLM response.

## `src/codeagent/memory/reconcile.py`

> 模块说明：Phase 2 of mem0-style memory writing: reconcile facts against the store. Newly extracted facts are compared with related existing memories, and the model decides the operations: ADD new ones, UPDATE memories the facts refine or contradict, DELETE memories that are no longer true. This keeps the store free of duplicated or mutually contradictory entries.

### 类 `MemoryOperation`

- **基类**：`object`
- **定义行**：47
- **功能说明**：`MemoryOperation`：该模块的数据结构或服务类型，详见源码 docstring 与调用处。

### 类 `MemoryReconciler`

- **基类**：`object`
- **定义行**：53
- **功能说明**：`MemoryReconciler`：该模块的数据结构或服务类型，详见源码 docstring 与调用处。

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def __init__(self, provider: LLMProvider) -> None` | 54 | 构造并保存依赖（Provider、路径、配置等）。 |
| `async def reconcile(self, facts: list[str], existing: list[Memory]) -> list[MemoryOperation]` | 57 | `reconcile`：该模块的公开接口，参数见签名，行为见同行源码。 |

## `src/codeagent/memory/smart.py`

> 模块说明：SmartMemoryStore: mem0-style two-phase writes over any MemoryStore. ``add()`` runs fact extraction → reconciliation → applies ADD/UPDATE/DELETE operations, so the store stays canonical instead of accumulating raw, possibly contradictory entries. It implements the full MemoryStore interface by delegation, making it a drop-in replacement usable with ``Agent(memory=...)`` and ``memory_tools(...)``.

### 类 `SmartMemoryStore`

- **基类**：`MemoryStore`
- **定义行**：20
- **功能说明**：A MemoryStore wrapper with LLM-driven write reconciliation.

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def __init__(self, store: MemoryStore, provider: LLMProvider, recall_limit: int = 10) -> None` | 23 | 构造并保存依赖（Provider、路径、配置等）。 |
| `def inner(self) -> MemoryStore` | 35 | `inner`：该模块的公开接口，参数见签名，行为见同行源码。 |
| `async def add(self, content: str, metadata: dict[str, Any] | None = None) -> Memory` | 38 | 新增一条记录。 |
| `async def add_with_operations(self, content: str, metadata: dict[str, Any] | None = None) -> list[MemoryOperation]` | 54 | Full two-phase write; returns the operations that were applied. |
| `async def update(self, memory_id: str, content: str) -> Memory | None` | 90 | `update`：该模块的公开接口，参数见签名，行为见同行源码。 |
| `async def search(self, query: str, limit: int = 5) -> list[Memory]` | 93 | `search`：该模块的公开接口，参数见签名，行为见同行源码。 |
| `async def list(self, limit: int = 50) -> list[Memory]` | 96 | 列出集合中的条目。 |
| `async def delete(self, memory_id: str) -> bool` | 99 | 删除一条记录。 |

## `src/codeagent/memory/store.py`

> 模块说明：Long-term memory: persistent storage and retrieval across sessions. The store abstraction is deliberately small so production deployments can swap in mem0 / vector databases / graph stores (neo4j) without changing agent code.

### 类 `Memory`

- **基类**：`object`
- **定义行**：21
- **功能说明**：一条长期记忆。

### 类 `MemoryStore`

- **基类**：`ABC`
- **定义行**：28
- **功能说明**：Persistence backend for long-term agent memory.

| 方法 | 行 | 功能说明 |
|---|---|---|
| `async def add(self, content: str, metadata: dict[str, Any] | None = None) -> Memory` | 32 | Store a memory and return it. |
| `async def update(self, memory_id: str, content: str) -> Memory | None` | 36 | Replace a memory's content. Returns the updated memory, or None. |
| `async def search(self, query: str, limit: int = 5) -> list[Memory]` | 40 | Return the most relevant memories for ``query``. |
| `async def list(self, limit: int = 50) -> list[Memory]` | 44 | Return recent memories, newest first. |
| `async def delete(self, memory_id: str) -> bool` | 48 | Delete a memory by id. Returns True if it existed. |

### 类 `LocalMemoryStore`

- **基类**：`MemoryStore`
- **定义行**：60
- **功能说明**：JSON-file memory store with keyword-overlap retrieval. Zero external dependencies — a sensible default. For semantic search, implement :class:`MemoryStore` over an embedding index instead.

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def __init__(self, path: str | Path) -> None` | 67 | 构造并保存依赖（Provider、路径、配置等）。 |
| `async def add(self, content: str, metadata: dict[str, Any] | None = None) -> Memory` | 82 | 新增一条记录。 |
| `async def update(self, memory_id: str, content: str) -> Memory | None` | 92 | `update`：该模块的公开接口，参数见签名，行为见同行源码。 |
| `async def search(self, query: str, limit: int = 5) -> list[Memory]` | 100 | `search`：该模块的公开接口，参数见签名，行为见同行源码。 |
| `async def list(self, limit: int = 50) -> list[Memory]` | 114 | 列出集合中的条目。 |
| `async def delete(self, memory_id: str) -> bool` | 117 | 删除一条记录。 |

## `src/codeagent/memory/tools.py`

> 模块说明：Memory tools: let the agent save and recall long-term memories.

### 类 `MemorySaveTool`

- **基类**：`Tool`
- **定义行**：12
- **功能说明**：`MemorySaveTool`：该模块的数据结构或服务类型，详见源码 docstring 与调用处。

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def __init__(self, store: MemoryStore) -> None` | 28 | 构造并保存依赖（Provider、路径、配置等）。 |
| `async def execute(self, content: str, tags: str = '', **_) -> str` | 31 | 真正执行工具副作用并返回文本。 |

### 类 `MemorySearchTool`

- **基类**：`Tool`
- **定义行**：37
- **功能说明**：`MemorySearchTool`：该模块的数据结构或服务类型，详见源码 docstring 与调用处。

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def __init__(self, store: MemoryStore) -> None` | 49 | 构造并保存依赖（Provider、路径、配置等）。 |
| `async def execute(self, query: str, limit: int = 5, **_) -> str` | 52 | 真正执行工具副作用并返回文本。 |

### 类 `MemoryListTool`

- **基类**：`Tool`
- **定义行**：59
- **功能说明**：`MemoryListTool`：该模块的数据结构或服务类型，详见源码 docstring 与调用处。

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def __init__(self, store: MemoryStore) -> None` | 69 | 构造并保存依赖（Provider、路径、配置等）。 |
| `async def execute(self, limit: int = 20, **_) -> str` | 72 | 真正执行工具副作用并返回文本。 |

### `def memory_tools(store: MemoryStore) -> list[Tool]`

- **定义行**：79
- **功能说明**：The standard memory tool set bound to a store.

## `src/codeagent/releases.py`

> 模块说明：Release history: single source of truth for version info. Rendered by ``codeagent version`` / ``codeagent changelog`` and mirrored in the repo's CHANGELOG.md.

### 类 `Release`

- **基类**：`object`
- **定义行**：13
- **功能说明**：`Release`：该模块的数据结构或服务类型，详见源码 docstring 与调用处。

### `def latest() -> Release`

- **定义行**：186
- **功能说明**：`latest`：该模块的公开接口，参数见签名，行为见同行源码。

### `def get_release(version: str) -> Release | None`

- **定义行**：190
- **功能说明**：按键或标识读取一项。

### `def changelog_text() -> str`

- **定义行**：194
- **功能说明**：Render the full changelog as markdown-ish text, newest first.

## `src/codeagent/repair/__init__.py`

（无公开类或函数；文件以常量、类型别名或再导出为主。）

## `src/codeagent/repair/self_debug.py`

> 模块说明：Self-Debugging: iterative repair via execution feedback and rubber-ducking. Implements the loop from "Teaching Large Language Models to Self-Debug" (Chen et al., Google Research & UC Berkeley, ICLR 2024): 1. **Generation** — the agent produces code for the task. 2. **Execution feedback** — a verification command runs against the result. 3. **Explanation + fix** — on failure, the agent explains what the code does and why it fails (rubber-duck debugging), then applies a minimal fix. The loop terminates when verification passes or ``max_turns`` is reached. Conversation history is reused across turns, so failed attempts stay in context — the paper shows this notably improves sample efficiency.

### 类 `Attempt`

- **基类**：`object`
- **定义行**：42
- **功能说明**：`Attempt`：该模块的数据结构或服务类型，详见源码 docstring 与调用处。

### 类 `SelfDebugResult`

- **基类**：`object`
- **定义行**：49
- **功能说明**：`SelfDebugResult`：该模块的数据结构或服务类型，详见源码 docstring 与调用处。

### 类 `SelfDebugger`

- **基类**：`object`
- **定义行**：56
- **功能说明**：Runs an agent on a task, then iterates execution-feedback repairs. Parameters ---------- agent: The agent to drive. History is kept across turns so the model can compare against its failed predictions. max_turns: Maximum verification/repair rounds after the initial generation. cwd: Working directory for the verification command. timeout: Per-run timeout (seconds) for the verification command.

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def __init__(self, agent: Agent, max_turns: int = 5, cwd: str | Path = '.', timeout: int = 60) -> None` | 72 | 构造并保存依赖（Provider、路径、配置等）。 |
| `async def run(self, task: str, verify_command: str) -> SelfDebugResult` | 99 | Generate a solution, then repair it until ``verify_command`` passes. |

## `src/codeagent/security/__init__.py`

（无公开类或函数；文件以常量、类型别名或再导出为主。）

## `src/codeagent/security/policy.py`

> 模块说明：Permission policy and command risk classification. Inspired by Codex's Guardian: every tool call carries a risk level, calls above the auto-approval threshold go to an approval handler (e.g. an interactive prompt), and risky calls fail closed when no handler is set.

### 类 `RiskLevel`

- **基类**：`IntEnum`
- **定义行**：18
- **功能说明**：Ordered risk classification for tool calls.

### 类 `ApprovalDecision`

- **基类**：`str, Enum`
- **定义行**：27
- **功能说明**：批准、拒绝、本会话永远允许。

### 类 `PermissionPolicy`

- **基类**：`object`
- **定义行**：38
- **功能说明**：Decides whether a tool call may proceed. - Tools listed in ``always_deny`` are rejected outright. - Tools listed in ``always_allow`` skip further checks. - Calls at or below ``auto_approve_up_to`` are approved silently. - Anything riskier goes to ``handler``; without a handler it is denied (fail closed).

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def __init__(self, auto_approve_up_to: RiskLevel = RiskLevel.READ_ONLY, handler: ApprovalHandler | None = None, always_allow: set[str] | None = None, always_deny: set[str] | None = None) -> None` | 48 | 构造并保存依赖（Provider、路径、配置等）。 |
| `def permissive(cls) -> PermissionPolicy` | 61 | Approve everything, including destructive calls. Use with care. |
| `def strict(cls) -> PermissionPolicy` | 66 | Auto-approve read-only calls only; deny the rest (no handler). |
| `async def authorize(self, call: ToolCall, risk: RiskLevel) -> ApprovalDecision` | 70 | `authorize`：该模块的公开接口，参数见签名，行为见同行源码。 |

### `def classify_command(command: str) -> RiskLevel`

- **定义行**：142
- **功能说明**：Heuristic risk classification for a shell command string.

## `src/codeagent/settings.py`

> 模块说明：Personalization: host-wide extra instructions & context for every chat. Stored at ``~/.codeagent/settings.json``; loaded once and injected into the system prompt of every agent on this host — chat, run, lead, workers, voice.

### 类 `Settings`

- **基类**：`object`
- **定义行**：17
- **功能说明**：Host-wide personalization applied to all agent conversations. - ``nickname``: how the agent should address the user. - ``language``: preferred reply language (e.g. "中文"). - ``instructions``: behavioral guidance ("回答要简洁", "先给结论"). - ``context``: facts about the user/host ("M4 Mac", "项目在 ~/code").

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def load(cls, path: str | Path | None = None) -> 'Settings'` | 36 | 从磁盘或默认路径加载持久化数据。 |
| `def save(self, path: str | Path | None = None) -> Path` | 49 | 把当前对象写回本机配置文件。 |
| `def clear(cls, path: str | Path | None = None) -> bool` | 68 | `clear`：该模块的公开接口，参数见签名，行为见同行源码。 |
| `def is_empty(self) -> bool` | 75 | `is_empty`：该模块的公开接口，参数见签名，行为见同行源码。 |
| `def prompt_block(self) -> str` | 82 | Render as a system-prompt section; empty string when unset. |

## `src/codeagent/skills/__init__.py`

> 模块说明：Skill system: loadable SKILL.md packs + self-evolution.

（无公开类或函数；文件以常量、类型别名或再导出为主。）

## `src/codeagent/skills/evolve.py`

> 模块说明：Skill evolution: distill task experience into reusable skills. Inspired by SkillClaw (skills evolve from real interactions) and self-improving-agent (evolution rules): after a task completes, an LLM reflects on what worked and writes/updates a SKILL.md in the library, so the agent gets better at similar tasks next time.

### 类 `SkillEvolver`

- **基类**：`object`
- **定义行**：54
- **功能说明**：Reflects on completed tasks and grows a :class:`SkillLibrary`.

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def __init__(self, provider: LLMProvider, library: SkillLibrary, directory: str | Path, max_answer_chars: int = 3000) -> None` | 57 | 构造并保存依赖（Provider、路径、配置等）。 |
| `async def evolve(self, task: str, answer: str) -> Skill | None` | 69 | Distill a finished task into a skill. Returns it, or None. |

## `src/codeagent/skills/fusion.py`

> 模块说明：Community skill fusion pack. Each entry is an original, compact distillation of a public GitHub project (cited in ``source``). We do not vendor their full SKILL.md trees.

### `def fusion_library() -> SkillLibrary`

- **定义行**：430
- **功能说明**：`fusion_library`：该模块的公开接口，参数见签名，行为见同行源码。

### `def install_fusion_skills(directory: Path | None = None) -> list[str]`

- **定义行**：442
- **功能说明**：Write fusion packs into ~/.codeagent/skills (overwrite same names).

### `def ensure_fusion_skills(directory: Path | None = None) -> list[str]`

- **定义行**：472
- **功能说明**：Write the fusion pack the app ships (refresh on every launch).

## `src/codeagent/skills/runtime.py`

> 模块说明：Studio skill runtime: recognize work, auto-enable, let the model invoke any skill.

### 类 `UseSkillTool`

- **基类**：`Tool`
- **定义行**：167
- **功能说明**：Let the model independently load any catalogued skill.

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def __init__(self, library: SkillLibrary, activate: Callable[[list[str]], list[Skill]] | None = None) -> None` | 192 | 构造并保存依赖（Provider、路径、配置等）。 |
| `async def execute(self, names: Any = None, name: Any = None, **_) -> str` | 229 | 真正执行工具副作用并返回文本。 |

### `def expand_work_query(query: str) -> str`

- **定义行**：97
- **功能说明**：Turn colloquial / CJK task text into searchable skill tokens.

### `def workspace_skill_hints(root: str | Path | None = None, extra: str = '') -> str`

- **定义行**：109
- **功能说明**：Describe the current project so skill search can see file types.

### `def match_work_skills(library: SkillLibrary, query: str, hints: str = '', limit: int = 8) -> list[Skill]`

- **定义行**：154
- **功能说明**：Pick skills for this task + workspace. Empty query still uses hints.

## `src/codeagent/skills/skill.py`

> 模块说明：Agent skills: reusable instruction packs in SKILL.md format. A skill is a markdown file with YAML-ish frontmatter:: --- name: pytest-patterns description: Battle-tested pytest patterns for fixtures and parametrization --- # Pytest Patterns ... instructions the agent should follow ... This is the format used by community skill packs (taste-skill, darwin-skill, gsap-skills, impeccable, qaskills, ponytail, ...), so any of them can be dropped into a skills directory and loaded by :class:`SkillLibrary`.

### 类 `Skill`

- **基类**：`object`
- **定义行**：27
- **功能说明**：A single loaded skill.

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def parse(cls, text: str, path: Path | None = None) -> 'Skill'` | 37 | Parse SKILL.md text (frontmatter + markdown body). |
| `def to_markdown(self) -> str` | 62 | Serialize back to SKILL.md format. |

### 类 `SkillLibrary`

- **基类**：`object`
- **定义行**：73
- **功能说明**：A collection of skills loaded from one or more directories.

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def __init__(self, skills: list[Skill] | None = None) -> None` | 76 | 构造并保存依赖（Provider、路径、配置等）。 |
| `def add(self, skill: Skill) -> Skill` | 81 | Add or replace a skill (keyed by name). |
| `def get(self, name: str) -> Skill | None` | 86 | 按键或标识读取一项。 |
| `def remove(self, name: str) -> bool` | 89 | `remove`：该模块的公开接口，参数见签名，行为见同行源码。 |
| `def load(cls, *directories) -> 'SkillLibrary'` | 99 | Load every ``SKILL.md`` found under the given directories. Layout is flexible: both ``dir/SKILL.md`` and ``dir/<skill-name>/SKILL.md`` are picked up. |
| `def save(self, skill: Skill, directory: str | Path) -> Path` | 114 | Persist a skill to ``directory/<name>/SKILL.md`` and register it. |
| `def names(self) -> list[str]` | 124 | `names`：该模块的公开接口，参数见签名，行为见同行源码。 |
| `def search(self, query: str, limit: int = 5) -> list[Skill]` | 127 | Keyword-overlap search over name, triggers, description, content. |
| `def catalog_block(self) -> str` | 150 | Compact index of every skill (name + description). |
| `def prompt_block(self, max_chars: int = 18000, query: str = '', active: list[str] | None = None) -> str` | 163 | Render skills as a system-prompt block. Includes an index of every skill (name + description) plus the full body of as many skills as fit within ``max_chars``. When ``query`` is set, matching skills are injected first. ``active`` names (auto-routed or use_skill) always come before the rest. |

## `src/codeagent/tools/__init__.py`

### `def default_tools(root_dir: str | Path = '.', restrict_to_root: bool = True, shell_timeout: int = 120) -> ToolRegistry`

- **定义行**：39
- **功能说明**：A registry pre-loaded with the built-in coding tools.

## `src/codeagent/tools/base.py`

> 模块说明：Tool abstraction and registry.

### 类 `Tool`

- **基类**：`ABC`
- **定义行**：13
- **功能说明**：A capability the agent can invoke. Subclasses set ``name``, ``description`` and ``parameters`` (a JSON Schema object describing the arguments) and implement :meth:`execute`. ``risk_level`` feeds the permission policy; override :meth:`risk_for` to classify per-call (e.g. based on the actual command being run).

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def risk_for(self, arguments: dict[str, Any]) -> RiskLevel` | 27 | `risk_for`：该模块的公开接口，参数见签名，行为见同行源码。 |
| `async def execute(self, **arguments) -> str` | 31 | Run the tool and return its output as a string. |
| `def to_schema(self) -> dict[str, Any]` | 34 | `to_schema`：该模块的公开接口，参数见签名，行为见同行源码。 |

### 类 `ToolRegistry`

- **基类**：`object`
- **定义行**：42
- **功能说明**：Holds the tools available to an agent and dispatches calls to them.

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def __init__(self, tools: list[Tool] | None = None) -> None` | 45 | 构造并保存依赖（Provider、路径、配置等）。 |
| `def register(self, tool: Tool) -> None` | 50 | `register`：该模块的公开接口，参数见签名，行为见同行源码。 |
| `def get(self, name: str) -> Tool | None` | 53 | 按键或标识读取一项。 |
| `def schemas(self) -> list[dict[str, Any]]` | 59 | `schemas`：该模块的公开接口，参数见签名，行为见同行源码。 |
| `async def execute(self, call: ToolCall) -> ToolResult` | 62 | 真正执行工具副作用并返回文本。 |

## `src/codeagent/tools/browser.py`

> 模块说明：Drive the user's real browser via BrowserSkill (bsk) or ego-lite. Does not vendor those projects. The agent calls this tool; we shell out to whatever bridge is already installed on the machine.

### 类 `BrowserTool`

- **基类**：`Tool`
- **定义行**：57
- **功能说明**：开页、点击、填表（BrowserSkill / ego-lite）。

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def __init__(self, session_path: str | Path | None = None, default_timeout: int = 90) -> None` | 93 | 构造并保存依赖（Provider、路径、配置等）。 |
| `def risk_for(self, arguments: dict[str, Any]) -> RiskLevel` | 101 | `risk_for`：该模块的公开接口，参数见签名，行为见同行源码。 |
| `async def execute(self, action: str, url: str | None = None, target: str | None = None, value: str | None = None, script: str | None = None, out: str | None = None, session: str | None = None, timeout: int | None = None, **_) -> str` | 107 | 真正执行工具副作用并返回文本。 |

### `def detect_browser_backend(which: Any = None) -> str`

- **定义行**：35
- **功能说明**：Return 'bsk', 'ego', or ''.

### `def parse_bsk_session_id(text: str) -> str`

- **定义行**：45
- **功能说明**：Pick the 4-letter session id BrowserSkill prints on start.

### `def browser_tools() -> list[Tool]`

- **定义行**：307
- **功能说明**：`browser_tools`：该模块的公开接口，参数见签名，行为见同行源码。

## `src/codeagent/tools/comfyui.py`

> 模块说明：Agent tool for a local ComfyUI server (https://github.com/Comfy-Org/ComfyUI).

### 类 `ComfyTool`

- **基类**：`Tool`
- **定义行**：23
- **功能说明**：向 ComfyUI queue API 工作流。

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def risk_for(self, arguments: dict[str, Any]) -> RiskLevel` | 59 | `risk_for`：该模块的公开接口，参数见签名，行为见同行源码。 |
| `async def execute(self, action: str, workflow_path: str = '', prompt: str = '', base: str = '', dest_dir: str = '', **_) -> str` | 64 | 真正执行工具副作用并返回文本。 |

### `def comfy_tools() -> list[Tool]`

- **定义行**：136
- **功能说明**：`comfy_tools`：该模块的公开接口，参数见签名，行为见同行源码。

## `src/codeagent/tools/delegate.py`

> 模块说明：Sub-agent delegation with usage roll-up to the parent's budget.

### 类 `DelegateTool`

- **基类**：`Tool`
- **定义行**：14
- **功能说明**：Spawn a fresh sub-agent for a self-contained subtask. The sub-agent starts with clean history; its token usage rolls up into the parent agent's totals, so root budgets account for delegation (the same accounting Codex applies to subagents and root goals).

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def __init__(self, agent_factory: Callable[[], 'Agent'], parent: 'Agent') -> None` | 40 | 构造并保存依赖（Provider、路径、配置等）。 |
| `async def execute(self, task: str, **_) -> str` | 44 | 真正执行工具副作用并返回文本。 |

## `src/codeagent/tools/filesystem.py`

> 模块说明：Filesystem tools: read, write, edit and list files within a root directory.

### 类 `_FilesystemTool`

- **基类**：`Tool`
- **定义行**：14
- **功能说明**：`_FilesystemTool`：该模块的数据结构或服务类型，详见源码 docstring 与调用处。

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def __init__(self, root_dir: str | Path = '.', restrict_to_root: bool = True) -> None` | 15 | 构造并保存依赖（Provider、路径、配置等）。 |

### 类 `ReadFileTool`

- **基类**：`_FilesystemTool`
- **定义行**：33
- **功能说明**：读工作区内文件。

| 方法 | 行 | 功能说明 |
|---|---|---|
| `async def execute(self, path: str, offset: int = 1, limit: int | None = None, **_) -> str` | 49 | 真正执行工具副作用并返回文本。 |

### 类 `WriteFileTool`

- **基类**：`_FilesystemTool`
- **定义行**：68
- **功能说明**：新建或覆盖文件。

| 方法 | 行 | 功能说明 |
|---|---|---|
| `async def execute(self, path: str, content: str, **_) -> str` | 84 | 真正执行工具副作用并返回文本。 |

### 类 `EditFileTool`

- **基类**：`_FilesystemTool`
- **定义行**：91
- **功能说明**：按旧/新字符串替换文件片段。

| 方法 | 行 | 功能说明 |
|---|---|---|
| `async def execute(self, path: str, old_string: str, new_string: str, replace_all: bool = False, **_) -> str` | 109 | 真正执行工具副作用并返回文本。 |

### 类 `ListDirTool`

- **基类**：`_FilesystemTool`
- **定义行**：134
- **功能说明**：列目录。

| 方法 | 行 | 功能说明 |
|---|---|---|
| `async def execute(self, path: str = '.', **_) -> str` | 144 | 真正执行工具副作用并返回文本。 |

## `src/codeagent/tools/ocr.py`

> 模块说明：OCR tool: extract text from images via tesseract or EasyOCR. Engine resolution order (``engine="auto"``): 1. pytesseract + tesseract binary (fast, system package) 2. EasyOCR (pure pip install, deep-learning based, no system binary)

### 类 `OCRTool`

- **基类**：`Tool`
- **定义行**：24
- **功能说明**：图像文字识别。

| 方法 | 行 | 功能说明 |
|---|---|---|
| `async def execute(self, image_path: str, lang: str = 'eng', engine: str = 'auto') -> str` | 54 | 真正执行工具副作用并返回文本。 |

### `def ocr_tools() -> list[Tool]`

- **定义行**：109
- **功能说明**：`ocr_tools`：该模块的公开接口，参数见签名，行为见同行源码。

## `src/codeagent/tools/search.py`

> 模块说明：Codebase search tools: regex content search and glob file matching.

### 类 `GrepTool`

- **基类**：`Tool`
- **定义行**：34
- **功能说明**：正则搜索文件内容。

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def __init__(self, root_dir: str | Path = '.') -> None` | 50 | 构造并保存依赖（Provider、路径、配置等）。 |
| `async def execute(self, pattern: str, path: str = '.', include: str | None = None, **_) -> str` | 53 | 真正执行工具副作用并返回文本。 |

### 类 `GlobTool`

- **基类**：`Tool`
- **定义行**：76
- **功能说明**：按文件名模式匹配。

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def __init__(self, root_dir: str | Path = '.') -> None` | 90 | 构造并保存依赖（Provider、路径、配置等）。 |
| `async def execute(self, pattern: str, **_) -> str` | 93 | 真正执行工具副作用并返回文本。 |

## `src/codeagent/tools/shell.py`

> 模块说明：Shell command execution tool.

### 类 `BashTool`

- **基类**：`Tool`
- **定义行**：15
- **功能说明**：执行 Shell，风险由 classify_command 决定。

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def __init__(self, cwd: str | Path = '.', default_timeout: int = 120) -> None` | 31 | 构造并保存依赖（Provider、路径、配置等）。 |
| `def risk_for(self, arguments: dict[str, Any]) -> RiskLevel` | 35 | `risk_for`：该模块的公开接口，参数见签名，行为见同行源码。 |
| `async def execute(self, command: str, timeout: int | None = None, **_) -> str` | 38 | 真正执行工具副作用并返回文本。 |

## `src/codeagent/tools/web.py`

> 模块说明：Web scraping tools powered by Scrapling. Two agent-facing tools: - ``web_fetch``: fetch a page and return readable text (or CSS-selected fragments) — the agent's window onto the live web. - ``web_scrape``: extract structured data with named CSS selectors. Both use Scrapling's ``AsyncFetcher`` (browser-grade TLS fingerprint via curl_cffi). ``stealth=True`` routes through ``StealthyFetcher`` (camoufox browser) to bypass anti-bot systems like Cloudflare — heavier, opt-in. Requires ``pip install codeagent[scrape]``.

### 类 `WebFetchTool`

- **基类**：`Tool`
- **定义行**：61
- **功能说明**：抓网页正文。

| 方法 | 行 | 功能说明 |
|---|---|---|
| `async def execute(self, url: str, selector: str | None = None, max_chars: int = 8000, stealth: bool = False, timeout: int = 30, **_) -> str` | 80 | 真正执行工具副作用并返回文本。 |

### 类 `WebScrapeTool`

- **基类**：`Tool`
- **定义行**：100
- **功能说明**：按 CSS 选择器抽结构化字段。

| 方法 | 行 | 功能说明 |
|---|---|---|
| `async def execute(self, url: str, selectors: dict[str, str], limit: int = 20, stealth: bool = False, timeout: int = 30, **_) -> str` | 122 | 真正执行工具副作用并返回文本。 |

### `def web_tools() -> list[Tool]`

- **定义行**：137
- **功能说明**：`web_tools`：该模块的公开接口，参数见签名，行为见同行源码。

## `src/codeagent/videoops/__init__.py`

> 模块说明：Video generation, editing, ops analysis, and publish drafts. Fuses LibTV / Remotion / 中文剪辑 / 短剧剧本 / 运营分析 / 多平台发布 into one local workspace. Publishing never auto-clicks platform buttons.

### 类 `VideoOpsConfig`

- **基类**：`object`
- **定义行**：73
- **功能说明**：`VideoOpsConfig`：该模块的数据结构或服务类型，详见源码 docstring 与调用处。

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def load(cls, path: Path | None = None) -> 'VideoOpsConfig'` | 86 | 从磁盘或默认路径加载持久化数据。 |
| `def save(self, path: Path | None = None) -> None` | 97 | 把当前对象写回本机配置文件。 |
| `def workspace(self) -> Path` | 105 | `workspace`：该模块的公开接口，参数见签名，行为见同行源码。 |

### `def bundled_library() -> SkillLibrary`

- **定义行**：109
- **功能说明**：`bundled_library`：该模块的公开接口，参数见签名，行为见同行源码。

### `def install_bundled_skills(directory: Path | None = None) -> list[str]`

- **定义行**：116
- **功能说明**：Write bundled packs into ~/.codeagent/skills (overwrite same names).

### `def load_all_skills(*directories) -> SkillLibrary`

- **定义行**：129
- **功能说明**：Video packs + fusion packs, then each directory (later files override).

### `def is_workspace_ready(root: Path) -> bool`

- **定义行**：145
- **功能说明**：`is_workspace_ready`：该模块的公开接口，参数见签名，行为见同行源码。

### `def bootstrap_workspace(root: Path) -> dict[str, Any]`

- **定义行**：149
- **功能说明**：`bootstrap_workspace`：该模块的公开接口，参数见签名，行为见同行源码。

### `def append_pipeline(root: Path, event: str) -> None`

- **定义行**：177
- **功能说明**：`append_pipeline`：该模块的公开接口，参数见签名，行为见同行源码。

### `def save_publish_draft(root: Path, data: dict[str, Any]) -> dict[str, Any]`

- **定义行**：186
- **功能说明**：把当前对象写回本机配置文件。

### `def toolchain_status() -> dict[str, Any]`

- **定义行**：201
- **功能说明**：`toolchain_status`：该模块的公开接口，参数见签名，行为见同行源码。

### `def workspace_status(root: Path) -> dict[str, Any]`

- **定义行**：219
- **功能说明**：`workspace_status`：该模块的公开接口，参数见签名，行为见同行源码。

## `src/codeagent/videoops/bundled.py`

> 模块说明：Bundled SKILL.md packs for video generation / publish / ops. Workflows are distilled from public open-source projects (LibTV CLI, Remotion skills, video-use-zh, short-drama, DeepAnalyze, auto-publish, AutoPublish) into CodeCoreAgent-owned instruction packs. Do not paste upstream repositories verbatim.

（无公开类或函数；文件以常量、类型别名或再导出为主。）

## `src/codeagent/videoops/cloud.py`

> 模块说明：Cloud / local HTTP video backends: MiniMax Hailuo, Kimi, ComfyUI. MiniMax Hailuo uses async ``/v1/video_generation``. Kimi/Moonshot is tried as OpenAI-style ``/v1/videos`` then MiniMax-shaped ``/v1/video_generation`` on the same host (some consoles expose a video model there).

### `def api_origin(base_url: str) -> str`

- **定义行**：27
- **功能说明**：``https://host/v1`` → ``https://host``.

### `def is_video_api_model(model: str = '', base_url: str = '', provider: str = '') -> bool`

- **定义行**：35
- **功能说明**：True when the model *name* is a video generator (not a chat LLM).

### `def video_backend(model: str = '', base_url: str = '', provider: str = '') -> str`

- **定义行**：44
- **功能说明**：`video_backend`：该模块的公开接口，参数见签名，行为见同行源码。

### `def list_video_credentials() -> list[dict[str, str]]`

- **定义行**：73
- **功能说明**：列出集合中的条目。

### `def pick_credential(backend: str, preferred_model: str = '') -> dict[str, str] | None`

- **定义行**：89
- **功能说明**：`pick_credential`：该模块的公开接口，参数见签名，行为见同行源码。

### `def map_hailuo_resolution(resolution: str) -> str`

- **定义行**：106
- **功能说明**：`map_hailuo_resolution`：该模块的公开接口，参数见签名，行为见同行源码。

### `def map_duration(num_frames: int, duration: int | None = None) -> int`

- **定义行**：119
- **功能说明**：`map_duration`：该模块的公开接口，参数见签名，行为见同行源码。

### `async def generate_minimax_video(prompt: str, dest: Path, api_key: str, base_url: str, model: str = 'MiniMax-Hailuo-2.3', resolution: str = '720p', duration: int = 6) -> dict[str, Any]`

- **定义行**：147
- **功能说明**：`generate_minimax_video`：该模块的公开接口，参数见签名，行为见同行源码。

### `async def generate_kimi_video(prompt: str, dest: Path, api_key: str, base_url: str, model: str = '', resolution: str = '720p', duration: int = 6) -> dict[str, Any]`

- **定义行**：228
- **功能说明**：`generate_kimi_video`：该模块的公开接口，参数见签名，行为见同行源码。

### `async def generate_cloud_video(prompt: str, dest: Path, backend: str, resolution: str = '720p', num_frames: int = 60, duration: int | None = None, model: str = '') -> dict[str, Any]`

- **定义行**：295
- **功能说明**：`generate_cloud_video`：该模块的公开接口，参数见签名，行为见同行源码。

## `src/codeagent/videoops/comfy.py`

> 模块说明：Talk to a local ComfyUI server (default http://127.0.0.1:8188).

### `def normalize_comfy_base(base: str) -> str`

- **定义行**：16
- **功能说明**：`normalize_comfy_base`：该模块的公开接口，参数见签名，行为见同行源码。

### `def resolve_comfy_base(explicit: str = '') -> str`

- **定义行**：25
- **功能说明**：Configured address, else the usual local 8188.

### `def patch_workflow_prompt(workflow: dict[str, Any], prompt: str) -> int`

- **定义行**：38
- **功能说明**：Write ``prompt`` into the first positive text node (CLIPTextEncode).

### `async def interrupt_comfy(base: str, timeout: float = 5.0) -> dict[str, Any]`

- **定义行**：66
- **功能说明**：`interrupt_comfy`：该模块的公开接口，参数见签名，行为见同行源码。

### `async def probe_comfy(base: str, timeout: float = 3.0) -> dict[str, Any] | None`

- **定义行**：142
- **功能说明**：探测远端服务是否在线及能力。

### `async def queue_comfy_workflow(base: str, workflow: dict[str, Any], dest_dir: Path, timeout: float = 600.0) -> dict[str, Any]`

- **定义行**：212
- **功能说明**：POST API-format graph to /prompt, poll /history, download /view.

### `def load_workflow_file(path: str | Path) -> dict[str, Any]`

- **定义行**：287
- **功能说明**：从磁盘或默认路径加载持久化数据。

## `src/codeagent/videoops/gradio.py`

> 模块说明：LAN Gradio 文生视频（WAN-1.3B 等）。 Matches the Gradio-exported Python API: Client(url).predict(..., api_name="/generate_video")

### `def normalize_resolution(value: str) -> str`

- **定义行**：32
- **功能说明**：`normalize_resolution`：该模块的公开接口，参数见签名，行为见同行源码。

### `def parse_sse_complete(text: str) -> Any`

- **定义行**：47
- **功能说明**：Return the JSON payload of the last SSE ``complete`` event, or None.

### `def extract_file(payload: Any) -> dict[str, Any]`

- **定义行**：86
- **功能说明**：Unwrap Gradio FileData from predict / SSE complete payload.

### `def resolve_gradio_base(cfg: VideoOpsConfig | None = None) -> str`

- **定义行**：106
- **功能说明**：`resolve_gradio_base`：该模块的公开接口，参数见签名，行为见同行源码。

### `async def probe_gradio_app(base: str, timeout: float = 4.0) -> dict[str, Any] | None`

- **定义行**：122
- **功能说明**：探测远端服务是否在线及能力。

### `async def generate_wan_video(base: str, prompt: str, resolution: str = '720p', num_frames: int = 60, num_inference_steps: int = 50, dest: Path | None = None, timeout: float = 600.0) -> dict[str, Any]`

- **定义行**：255
- **功能说明**：`generate_wan_video`：该模块的公开接口，参数见签名，行为见同行源码。

## `src/codeagent/videoops/studio.py`

> 模块说明：Director desk: script → shots → Comfy/WAN/cloud generate → ffmpeg assemble. Follows ComfyUI local API (https://docs.comfy.org/zh , https://github.com/Comfy-Org/ComfyUI): Save (API Format) JSON → POST /prompt → GET /history/{id} → GET /view. Comfy is a node graph, not a chat model.

### 类 `Shot`

- **基类**：`object`
- **定义行**：40
- **功能说明**：导演台一镜。

### 类 `Desk`

- **基类**：`object`
- **定义行**：60
- **功能说明**：导演台整场：企划 + 分镜 + 成片路径。

### `def desk_root(workspace: Path | None = None) -> Path`

- **定义行**：90
- **功能说明**：`desk_root`：该模块的公开接口，参数见签名，行为见同行源码。

### `def desk_path(workspace: Path | None = None) -> Path`

- **定义行**：96
- **功能说明**：`desk_path`：该模块的公开接口，参数见签名，行为见同行源码。

### `def load_desk(workspace: Path | None = None) -> Desk`

- **定义行**：100
- **功能说明**：从磁盘或默认路径加载持久化数据。

### `def save_desk(desk: Desk, workspace: Path | None = None) -> Path`

- **定义行**：114
- **功能说明**：把当前对象写回本机配置文件。

### `def ensure_desk_workspace() -> Path`

- **定义行**：128
- **功能说明**：`ensure_desk_workspace`：该模块的公开接口，参数见签名，行为见同行源码。

### `def desk_dict(desk: Desk) -> dict[str, Any]`

- **定义行**：146
- **功能说明**：`desk_dict`：该模块的公开接口，参数见签名，行为见同行源码。

### `def parse_script_shots(text: str) -> list[Shot]`

- **定义行**：150
- **功能说明**：Turn a script / shot list into director-desk shots.

### `def apply_desk_patch(desk: Desk, data: dict[str, Any]) -> Desk`

- **定义行**：176
- **功能说明**：`apply_desk_patch`：该模块的公开接口，参数见签名，行为见同行源码。

### `async def generate_one_shot(desk: Desk, shot_id: str) -> dict[str, Any]`

- **定义行**：205
- **功能说明**：Queue one shot through video_generate backends. Mutates and saves desk.

### `def assemble_desk(desk: Desk) -> dict[str, Any]`

- **定义行**：252
- **功能说明**：`assemble_desk`：该模块的公开接口，参数见签名，行为见同行源码。

## `src/codeagent/videoops/tools.py`

> 模块说明：Agent tools for the video ops workspace.

### 类 `VideoOpsStatusTool`

- **基类**：`Tool`
- **定义行**：20
- **功能说明**：`VideoOpsStatusTool`：该模块的数据结构或服务类型，详见源码 docstring 与调用处。

| 方法 | 行 | 功能说明 |
|---|---|---|
| `async def execute(self, **_) -> str` | 28 | 真正执行工具副作用并返回文本。 |

### 类 `VideoOpsLogTool`

- **基类**：`Tool`
- **定义行**：47
- **功能说明**：`VideoOpsLogTool`：该模块的数据结构或服务类型，详见源码 docstring 与调用处。

| 方法 | 行 | 功能说明 |
|---|---|---|
| `async def execute(self, event: str, **_) -> str` | 59 | 真正执行工具副作用并返回文本。 |

### 类 `VideoOpsDraftTool`

- **基类**：`Tool`
- **定义行**：68
- **功能说明**：`VideoOpsDraftTool`：该模块的数据结构或服务类型，详见源码 docstring 与调用处。

| 方法 | 行 | 功能说明 |
|---|---|---|
| `async def execute(self, **kwargs) -> str` | 87 | 真正执行工具副作用并返回文本。 |

### 类 `VideoGenerateTool`

- **基类**：`Tool`
- **定义行**：96
- **功能说明**：`VideoGenerateTool`：该模块的数据结构或服务类型，详见源码 docstring 与调用处。

| 方法 | 行 | 功能说明 |
|---|---|---|
| `async def execute(self, prompt: str, provider: str = 'auto', resolution: str = '720p', num_frames: int = 60, num_inference_steps: int = 50, duration: int | None = None, workflow_path: str = '', dest_dir: str = '', **_) -> str` | 134 | 真正执行工具副作用并返回文本。 |

### 类 `VideoStudioTool`

- **基类**：`Tool`
- **定义行**：230
- **功能说明**：`VideoStudioTool`：该模块的数据结构或服务类型，详见源码 docstring 与调用处。

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def risk_for(self, arguments: dict[str, Any]) -> RiskLevel` | 256 | `risk_for`：该模块的公开接口，参数见签名，行为见同行源码。 |
| `async def execute(self, action: str, title: str = '', logline: str = '', script: str = '', prompt: str = '', shot_id: str = '', engine: str = '', workflow_path: str = '', **_) -> str` | 261 | 真正执行工具副作用并返回文本。 |

### `def video_ops_tools(cfg: VideoOpsConfig | None = None) -> list[Tool]`

- **定义行**：349
- **功能说明**：`video_ops_tools`：该模块的公开接口，参数见签名，行为见同行源码。

## `src/codeagent/voice/__init__.py`

> 模块说明：Voice & emotion: emotional dialogue mode, continuous chat, free TTS.

（无公开类或函数；文件以常量、类型别名或再导出为主。）

## `src/codeagent/voice/asr.py`

> 模块说明：Speech recognition: provider abstraction plus free offline ASR. ``WhisperASRProvider`` runs faster-whisper locally — free, offline, no API key, good Chinese accuracy. This completes the voice loop (mic → ASR → agent → emotion TTS) without any cloud dependency.

### 类 `ASRProvider`

- **基类**：`ABC`
- **定义行**：15
- **功能说明**：Transcribe an audio file into text.

| 方法 | 行 | 功能说明 |
|---|---|---|
| `async def transcribe(self, audio_path: str | Path) -> str` | 19 | Return the recognized text (empty string if nothing heard). |

### 类 `WhisperASRProvider`

- **基类**：`ASRProvider`
- **定义行**：23
- **功能说明**：Local faster-whisper ASR (``pip install codeagent[voice-full]``). ``model_size``: tiny / base / small / medium / large-v3 — larger is more accurate but slower; ``base`` is a sensible laptop default.

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def __init__(self, model_size: str = 'base', language: str | None = 'zh') -> None` | 30 | 构造并保存依赖（Provider、路径、配置等）。 |
| `async def transcribe(self, audio_path: str | Path) -> str` | 52 | `transcribe`：该模块的公开接口，参数见签名，行为见同行源码。 |

## `src/codeagent/voice/chat.py`

> 模块说明：Continuous conversation loop with emotion and voice. Mirrors xiaozhi's auto mode (listen → think → speak → listen again) in a UI-agnostic form: the caller drives turns with user input; each turn runs the agent, parses the emotion tag, and speaks the reply through TTS. Conversation history persists across turns via the agent itself, so this is continuous dialogue, not one-shot Q&A.

### 类 `ChatTurn`

- **基类**：`object`
- **定义行**：22
- **功能说明**：`ChatTurn`：该模块的数据结构或服务类型，详见源码 docstring 与调用处。

### 类 `VoiceChatLoop`

- **基类**：`object`
- **定义行**：29
- **功能说明**：Drives multi-turn emotional (optionally voiced) conversation.

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def __init__(self, agent: Agent, tts: TTSProvider | None = None, exit_words: tuple[str, ...] = DEFAULT_EXIT_WORDS, auto_play: bool = True) -> None` | 32 | 构造并保存依赖（Provider、路径、配置等）。 |
| `def is_exit(self, user_text: str) -> bool` | 45 | `is_exit`：该模块的公开接口，参数见签名，行为见同行源码。 |
| `async def turn(self, user_text: str) -> ChatTurn | None` | 48 | Run one conversation turn. Returns None when the user exits. |

## `src/codeagent/voice/emotion.py`

> 模块说明：Emotion mode: xiaozhi-style out-of-band emotion tags. Inspired by xiaozhi-esp32's `{"type":"llm","emotion":"happy"}` channel: the model prefixes every reply with ``[emotion:happy]``; the tag is parsed off the text and mapped to a voice style (voice / rate / pitch) for TTS, so the agent speaks with emotional prosody.

### 类 `Emotion`

- **基类**：`str, Enum`
- **定义行**：16
- **功能说明**：`Emotion`：该模块的数据结构或服务类型，详见源码 docstring 与调用处。

### 类 `VoiceStyle`

- **基类**：`object`
- **定义行**：69
- **功能说明**：Prosody for one emotion. ``voice=None`` keeps the TTS default.

### `def parse_emotion(text: str) -> tuple[Emotion, str]`

- **定义行**：55
- **功能说明**：Split a leading ``[emotion:xxx]`` tag off the reply. Returns ``(emotion, clean_text)``; unknown or missing tags yield ``Emotion.NEUTRAL`` with the tag (if any) stripped.

### `def style_for(emotion: Emotion) -> VoiceStyle`

- **定义行**：90
- **功能说明**：`style_for`：该模块的公开接口，参数见签名，行为见同行源码。

## `src/codeagent/voice/recorder.py`

> 模块说明：Microphone recording for voice input. Push-to-talk style: recording runs until the caller signals stop (the CLI binds this to the Enter key), producing a 16 kHz mono WAV — the format Whisper expects.

### `async def record_until_enter(path: str | Path | None = None, sample_rate: int = SAMPLE_RATE) -> Path`

- **定义行**：18
- **功能说明**：Record from the default microphone until Enter is pressed. Requires ``sounddevice`` + ``soundfile`` (``pip install codeagent[voice-full]``). Returns the WAV file path.

## `src/codeagent/voice/speech.py`

> 模块说明：Voice Surface helpers distilled from LunarCore v3.3.17 (not a source dump).

### `def to_speech_text(text: str, max_len: int = 220) -> str`

- **定义行**：15
- **功能说明**：Strip markdown/code so TTS only speaks the conclusion and numbers.

### `def cute_style(pitch_hz: int = -10, rate_pct: int = -5, enabled: bool = True) -> VoiceStyle`

- **定义行**：27
- **功能说明**：LunarCore 嗲音：pitch in Hz, rate in percent (Edge TTS format).

## `src/codeagent/voice/tts.py`

> 模块说明：Text-to-speech: provider abstraction plus a free voice pack. ``EdgeTTSProvider`` uses Microsoft Edge's neural voices via ``edge-tts`` — completely free, no API key, with high-quality Chinese voices (Xiaoxiao / Xiaoyi / Yunxi / Yunjian). This is the default "voice pack"; production deployments can subclass :class:`TTSProvider` for Volcano Engine (what xiaozhi.me uses), CosyVoice, or ElevenLabs.

### 类 `TTSProvider`

- **基类**：`ABC`
- **定义行**：65
- **功能说明**：Synthesize text into an audio file.

| 方法 | 行 | 功能说明 |
|---|---|---|
| `async def synthesize(self, text: str, style: VoiceStyle | None = None) -> Path` | 69 | Return the path of a synthesized audio file. |

### 类 `EdgeTTSProvider`

- **基类**：`TTSProvider`
- **定义行**：73
- **功能说明**：Free neural-voice pack (requires ``pip install codeagent[voice]``). With ``emotion_voices=True`` (default), per-emotion styles may switch voices. Pass ``emotion_voices=False`` to pin one voice (e.g. the Taiwanese HsiaoChen) and let emotions only adjust rate/pitch.

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def __init__(self, voice: str = DEFAULT_VOICE, output_dir: str | Path | None = None, emotion_voices: bool = True) -> None` | 81 | 构造并保存依赖（Provider、路径、配置等）。 |
| `async def synthesize(self, text: str, style: VoiceStyle | None = None) -> Path` | 91 | `synthesize`：该模块的公开接口，参数见签名，行为见同行源码。 |

### `def resolve_voice(name_or_id: str) -> str`

- **定义行**：60
- **功能说明**：Accept a preset alias (``hsiaochen``) or a full edge-tts voice ID.

### `async def play_audio(path: str | Path) -> None`

- **定义行**：120
- **功能说明**：Play an audio file with whatever player the OS offers.

## `src/codeagent/xiaozhi/__init__.py`

> 模块说明：Xiaozhi client: connect codeagent to xiaozhi.me's free voice server.

（无公开类或函数；文件以常量、类型别名或再导出为主。）

## `src/codeagent/xiaozhi/audio.py`

> 模块说明：Opus codec for xiaozhi audio frames (16 kHz mono, 60 ms frames). Thin wrapper over opuslib. System dependency: libopus (macOS: ``brew install opus``; Debian: ``apt install libopus0``). Kept optional so the protocol layer works without audio hardware.

### 类 `OpusCodec`

- **基类**：`object`
- **定义行**：16
- **功能说明**：`OpusCodec`：该模块的数据结构或服务类型，详见源码 docstring 与调用处。

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def __init__(self) -> None` | 17 | 构造并保存依赖（Provider、路径、配置等）。 |
| `def encode(self, pcm: bytes) -> bytes` | 28 | PCM s16le (1920 bytes = 60 ms) → Opus frame. |
| `def decode(self, opus_frame: bytes) -> bytes` | 32 | Opus frame → PCM s16le. |

## `src/codeagent/xiaozhi/client.py`

> 模块说明：High-level xiaozhi client: OTA check-in + protocol + conversation events. Connects codeagent to xiaozhi.me's free server (Qwen realtime model + official voices) as a virtual device. Push-to-talk instead of wake word; audio frames flow through callbacks so any front-end (CLI, voice module) can plug in.

### 类 `ConversationEvents`

- **基类**：`object`
- **定义行**：21
- **功能说明**：Collected during one server turn (listen stop → tts stop).

### 类 `XiaozhiClient`

- **基类**：`object`
- **定义行**：30
- **功能说明**：小智协议高层客户端。

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def __init__(self, config: XiaozhiConfig | None = None, config_path: str | Path | None = None) -> None` | 31 | 构造并保存依赖（Provider、路径、配置等）。 |
| `async def connect(self) -> None` | 45 | OTA check-in (activation handling included), then WS handshake. |
| `async def say(self, opus_frames: list[bytes], mode: str = 'manual', timeout: float = 60) -> ConversationEvents` | 55 | One conversation turn: stream mic frames, collect the reply. Returns when the server finishes its TTS turn (or ``timeout`` hits). |
| `async def abort(self) -> None` | 79 | `abort`：该模块的公开接口，参数见签名，行为见同行源码。 |
| `async def close(self) -> None` | 82 | `close`：该模块的公开接口，参数见签名，行为见同行源码。 |

## `src/codeagent/xiaozhi/config.py`

> 模块说明：Device identity for the xiaozhi protocol. Mirrors xiaozhi-esp32: ``device_id`` is a MAC address, ``client_id`` a persistent UUID. Both are stored locally so the device stays registered across restarts.

### 类 `XiaozhiConfig`

- **基类**：`object`
- **定义行**：26
- **功能说明**：`XiaozhiConfig`：该模块的数据结构或服务类型，详见源码 docstring 与调用处。

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def load(cls, path: str | Path = DEFAULT_CONFIG_PATH) -> 'XiaozhiConfig'` | 32 | 从磁盘或默认路径加载持久化数据。 |
| `def save(self, path: str | Path = DEFAULT_CONFIG_PATH) -> None` | 45 | 把当前对象写回本机配置文件。 |

## `src/codeagent/xiaozhi/ota.py`

> 模块说明：OTA check-in: the entry point to xiaozhi.me's free server. POSTing device info to the OTA endpoint returns either: - ``websocket`` section (url + token) → device is activated, ready to connect; - ``activation`` section (code + message) → the user must register this device in the xiaozhi.me console with the shown code, then check in again.

### 类 `OtaResult`

- **基类**：`object`
- **定义行**：17
- **功能说明**：`OtaResult`：该模块的数据结构或服务类型，详见源码 docstring 与调用处。

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def activated(self) -> bool` | 25 | `activated`：该模块的公开接口，参数见签名，行为见同行源码。 |

### 类 `ActivationRequiredError`

- **基类**：`RuntimeError`
- **定义行**：29
- **功能说明**：Device not yet activated; user must register the code at xiaozhi.me.

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def __init__(self, result: OtaResult) -> None` | 32 | 构造并保存依赖（Provider、路径、配置等）。 |

### 类 `OtaClient`

- **基类**：`object`
- **定义行**：41
- **功能说明**：`OtaClient`：该模块的数据结构或服务类型，详见源码 docstring 与调用处。

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def __init__(self, config: XiaozhiConfig) -> None` | 42 | 构造并保存依赖（Provider、路径、配置等）。 |
| `async def check_in(self) -> OtaResult` | 45 | `check_in`：该模块的公开接口，参数见签名，行为见同行源码。 |

## `src/codeagent/xiaozhi/protocol.py`

> 模块说明：Xiaozhi WebSocket protocol (client side). Implements the device-side protocol from xiaozhi-esp32's docs/websocket.md: hello handshake → typed JSON control messages + binary Opus audio frames. client → hello → server hello client → listen(start) → binary opus frames → listen(stop) server → stt / tts(start|sentence_start|stop) / llm(emotion) / binary opus The layer is transport-pure: it parses and builds messages, dispatches via callbacks, and never touches audio hardware — so it is fully testable against a mock WebSocket server.

### 类 `XiaozhiMessage`

- **基类**：`object`
- **定义行**：27
- **功能说明**：A parsed server→client JSON message.

### 类 `XiaozhiProtocol`

- **基类**：`object`
- **定义行**：41
- **功能说明**：`XiaozhiProtocol`：该模块的数据结构或服务类型，详见源码 docstring 与调用处。

| 方法 | 行 | 功能说明 |
|---|---|---|
| `def __init__(self, on_message: MessageHandler | None = None, on_audio: AudioHandler | None = None) -> None` | 42 | 构造并保存依赖（Provider、路径、配置等）。 |
| `def hello_message() -> dict` | 55 | `hello_message`：该模块的公开接口，参数见签名，行为见同行源码。 |
| `def listen_message(self, state: str, mode: str = 'manual', text: str | None = None) -> dict` | 68 | `listen_message`：该模块的公开接口，参数见签名，行为见同行源码。 |
| `def abort_message(self, reason: str = 'wake_word_detected') -> dict` | 79 | `abort_message`：该模块的公开接口，参数见签名，行为见同行源码。 |
| `async def connect(self, url: str, token: str, device_id: str, client_id: str, proxy: 'str | None | bool' = True) -> None` | 84 | 建立网络或设备连接。 |
| `async def send_json(self, message: dict) -> None` | 118 | 发送用户消息并驱动 Agent。 |
| `async def send_audio(self, opus_frame: bytes) -> None` | 122 | 发送用户消息并驱动 Agent。 |
| `async def start_listening(self, mode: str = 'manual') -> None` | 126 | `start_listening`：该模块的公开接口，参数见签名，行为见同行源码。 |
| `async def stop_listening(self) -> None` | 129 | 请求停止当前生成。 |
| `async def abort(self, reason: str = 'wake_word_detected') -> None` | 132 | `abort`：该模块的公开接口，参数见签名，行为见同行源码。 |
| `async def receive_loop(self) -> None` | 135 | Dispatch incoming messages until the server disconnects. |
| `async def close(self) -> None` | 156 | `close`：该模块的公开接口，参数见签名，行为见同行源码。 |
